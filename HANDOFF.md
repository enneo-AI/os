# HANDOFF — Stand & nächste Schritte

**Zuletzt aktualisiert:** 2026-07-17 (kompakte Bibliothek und einheitliches Zugriffsmodell)

### Session 2026-07-17 — Persönlicher Lemlist-OAuth statt Credential-Formular

- **Ein Klick, echter Anbieter-Login:** Lemlist erscheint direkt unter `Read & Write`. `Verbinden` startet den offiziellen Remote-MCP-OAuth-Flow auf `app.lemlist.com`: Discovery, Dynamic Client Registration, PKCE, Team-Auswahl, Consent sowie Access-/Refresh-Token laufen ohne Lemlist-Passwort oder manuell kopierten API-Key.
- **Eindeutiger Owner:** Persönliche Connections gehören serverseitig ausnahmslos dem eingeloggten enneo-OS-Account. Das missverständliche Feld `Für Account` wurde auch für Admins entfernt; ein Admin kann keine persönlichen Credentials stellvertretend für andere speichern. Gemeinsame Service-Verbindungen bleiben ein expliziter `scope=team`-Sonderfall nur für Admins.
- **Sichere Persistenz:** Access-Token, Refresh-Token, dynamische OAuth-Client-Informationen und der kurzlebige PKCE-Verifier werden AES-256-GCM-verschlüsselt. Die neue service-only Tabelle `mcp_oauth_sessions` hat RLS, keine Grants für `anon`/`authenticated` und löscht den State nach Callback. Migration `20260717121000_mcp_oauth_personal_connections.sql` ist live und in der Migration History eingetragen.
- **Space-Grenze bleibt erhalten:** Nach erfolgreichem Login ist Lemlist weiterhin für Enni inaktiv, bis die konkrete persönliche Connection einem Open oder Restricted Space zugeordnet wird. Ein Reconnect übernimmt bestehende Space-Zuordnungen und ersetzt die alte Connection ohne Duplikat.
- **Produktiv verifiziert:** Offizieller Lemlist-Discovery-Smoke erzeugte `https://app.lemlist.com/oauth/authorize` mit dynamischer Client-ID, PKCE-Challenge und CSRF-State – einmal isoliert mit dem MCP-SDK, einmal über den echten `createMcpOAuthUrl`-Produktionscode und einmal über den live authentifizierten Backend-Endpunkt. Der Browser-Smoke bestätigte den vollständigen Marketplace-Klick bis zur echten `app.lemlist.com/login`-Seite samt Rücksprung auf den enneo-PKCE-Callback; bewusst keine fremden Lemlist-Credentials eingegeben. Railway-Deployment `0a55f851-de6e-4c14-bdb2-44a0a9650c13` ist `SUCCESS`, `/health` grün, Frontend live. JavaScript-Syntax, Diff-Check und Backend-Tests 14/14 grün; QA-Account und offene OAuth-Sessions anschließend vollständig entfernt.

### Session 2026-07-17 — Kompakte Bibliothek + Team-Zugänge

- **Eine Sprache für Zugriff:** Die Oberflächen für Spaces, Marketplace, Kontexte, Skills und Routinen verwenden nur noch `Open` und `Restricted`. Persönliche Inhalte sind kein dritter Modus, sondern `Restricted · nur du`. Spaces und Routinen unterstützen Restricted-Zugriff für mehrere ausgewählte Accounts; bei Kontexten und Skills ist Restricted aktuell noch auf den jeweiligen Owner begrenzt.
- **Codex-inspirierte Bibliothek:** Der frühere Bereich „Enni konfigurieren“ heißt `Bibliothek`. Marketplace, Kontexte, Skills und Routinen verwenden kompakte Header, Icon-Aktionen, Suche, Access-Filter und zweispaltige Listen statt langer Erklärblöcke und rein vertikaler Karten.
- **Provider, Account und Enni-Zugriff getrennt:** Ein Admin kann eine App wie Outlook organisationsweit bereitstellen, jeder Nutzer verbindet darin seinen eigenen Account, und erst die Zuordnung der konkreten Connection zu einem Open oder Restricted Space aktiviert sie für Enni. Der Marketplace zeigt verbundene Accounts kompakt, vergibt aber selbst keine Ausführungsrechte.
- **Team-Rollout:** Die bestehenden Accounts von Murat Alkan, Diana Schröter und Mehmet Caliskan erhielten neue einmalige Startpasswörter; Christ Kilchling wurde neu angelegt. Alle vier sind aktive Nicht-Admins mit zurückgesetztem Onboarding und Produkttour. Startpasswörter werden bewusst nicht in Git oder Handoff gespeichert.
- **Verifikation und live:** Backend-Tests 13/13, JavaScript-Syntax und Diff-Check grün. Marketplace und Skill-Bibliothek wurden mit einem echten temporären Admin-Account bei 1440×900 und 390×844 gegen Production geprüft: zweispaltige Desktop-Listen, einspaltige Mobile-Liste und kein horizontaler Overflow. Der Testaccount und alle Browser-QA-Sitzungen wurden anschließend entfernt. Netlify liefert die UI-Commits `9fab6cc` und `a56b691`; `/`, `/spaces`, `/spaces/marketplace`, `/spaces/kontexte`, `/spaces/skills` und `/spaces/routinen` antworten auf `os.enneo.ai` mit HTTP 200.
- **Bewusster nächster Architekturpunkt:** Wenn Restricted künftig auch für mehrere Personen bei Kontexten und Skills gelten soll, sollte die Berechtigung nicht über neue Einzel-Selektoren dupliziert werden. Empfohlen ist ein einheitliches Asset→Space-Modell: Open-/Restricted-Space ist die einzige Access Boundary; Connectoren, Seiten, Kontexte, Skills und Routinen werden einem Space zugeordnet. Dafür sind Migration, RLS, Backend-Discovery und UI-Zuordnung gemeinsam umzubauen.

### Session 2026-07-17 — Marketplace speichert, Spaces autorisieren

- **Klare Objektgrenze:** Der Marketplace heißt wieder sichtbar `Marketplace` und dient ausschließlich zum Verbinden und Verwalten von Connections. Eine gespeicherte Connection ist für Enni zunächst inaktiv; die UI sagt das nach OAuth und MCP-Verbindungen ausdrücklich.
- **Konkrete Connection statt Anbieter-Platzhalter:** `space_connections.connection_key` referenziert externe Verbindungen als `connector:<uuid>`. Damit aktiviert ein Space exakt ein Credential-Set und nicht pauschal „Outlook“, „Slack“ oder einen anderen Provider.
- **Open vs. Restricted technisch erzwungen:** Backend-Tool-Discovery lädt Connectoren nur, wenn sie mindestens einem zugänglichen Space zugeordnet sind. Open Space bedeutet alle aktiven Accounts; Restricted Space bedeutet ausschließlich Ersteller und explizite Mitglieder. Admin-Status allein gibt keinen Restricted-Toolzugriff. MCP-Ausführungen prüfen die Berechtigung erneut unmittelbar vor jedem Tool-Call.
- **Marketplace-Status ist nicht Toolzugriff:** `personal/team/proposed` steuert weiterhin Katalogsichtbarkeit und Freigabe, aber nie mehr die Ausführungsberechtigung. Ein persönliches Credential kann bewusst über einen Open oder Restricted Space geteilt werden; ein unzugeordnetes Credential bleibt selbst für seinen Owner aus Ennis Toolkatalog entfernt.
- **Sicheres Space-Management:** Nur der Space-Ersteller darf Connections in Restricted Spaces aktivieren oder entfernen. Bei Open Spaces dürfen zusätzlich aktive Admins verwalten. Private `SECURITY DEFINER`-Helper mit internem `auth.uid()` vermeiden die vorherige `spaces ↔ space_members`-RLS-Rekursion; die Funktionen liegen nicht im exponierten `public`-Schema.
- **Bestandsverhalten bewusst erhalten:** Die zwei bestehenden Team-Connections DeepWiki und Attio wurden bei der Migration einmalig `Company Data` als Open Space zugeordnet. Neue Connections erhalten niemals automatisch eine Space-Zuordnung. Reconnects übertragen vorhandene Space-Zuordnungen auf die neue Connector-ID; Trennen entfernt verwaiste Zuordnungen.
- **Verifiziert und live:** Migration `20260717082825_space_scoped_connectors.sql` ist live und in der Migration History. Ein echter 3-Account-Production-Eval bestätigte: Owner erhält Open + eigenes Restricted, eingeladenes Mitglied erhält Open + Restricted, Outsider nur Open, unzugeordnete Connection bleibt auch für Owner unsichtbar, Outsider sieht weder Restricted Space noch Connector-Metadaten und kann keine fremde Zuordnung anlegen. Temporäre Accounts, Spaces und Connectoren wurden entfernt. Backend-Tests 13/13, JavaScript-Syntax und Diff-Check grün. Railway-Deployment `e3cb0b3c-75ef-4135-8c48-c6f532b2f25c` ist `SUCCESS`, `/health` grün und Netlify liefert Commit `f493423` mit Marketplace-/Space-UI aus.

### Session 2026-07-17 — Recherchierte Connectoren im bestehenden Marketplace

- **Keine Sonderrubrik:** Freigegebene Research-Connectoren erscheinen direkt unter `Read & Write` oder `Read-only`; die zusätzliche Überschrift „Von Enni recherchiert“ wurde entfernt.
- **Echte Anbieterlogos:** Der Research-Blueprint speichert eine verifizierte HTTPS-Logo-/Favicon-URL. Metadaten der offiziellen Quellen haben Vorrang, ein Domain-Favicon dient als deterministischer Fallback. TickTick wurde live auf das TickTick-Logo von Simple Icons zurückgefüllt.
- **Nur zwei verständliche Zugriffsarten:** Neue Blueprints erlauben ausschließlich `read_only` und `read_write`. Sobald irgendeine Capability erstellt, verändert oder löscht, landet die Integration in `Read & Write`. Marketplace-Karte und Detailansicht zeigen den Zugriff ausdrücklich.
- **Verifiziert und live:** JavaScript-Syntax, Diff-Check und Backend-Tests 12/12 grün. Production-Browserprüfung bestätigt TickTick unter `Read & Write`, geladenes Logo (`150 px` intrinsische Breite) und keine alte Sonderüberschrift. Railway-Deployment `eab43eba-8f57-4caf-b25d-8eef9c6e6c2f` ist `SUCCESS`; Netlify liefert Commit `c9a5d82`. Temporärer Testaccount wurde entfernt.

### Session 2026-07-17 — Context Foundation aus Tristan-Feedback

- **Multi-Abteilungen:** Profile haben jetzt `departments[]` plus eine weiterhin kompatible primäre `department`. Onboarding und Profil erlauben Mehrfachauswahl; Finance ist ein eigener Bereich. Team-Gruppierung verwendet weiterhin das primäre Label.
- **Privates Rollen-Interview:** Das Pflicht-Onboarding umfasst fünf Schritte: Identität, Abteilungen/Rolle, Verantwortung/Arbeitspräferenzen/Engpässe, Ziele für 3/6/12 Monate und persönliches Passwort. Aus den Antworten entsteht ein geschützter `personal_profile`-Kontext, der weder anderen Mitgliedern noch Admins über RLS sichtbar ist. Enni lädt ihn serverseitig pro Turn und verwendet sensible Details nur still zur Personalisierung.
- **Kontext-Bibliothek:** Neuer Bereich `Spaces → Kontexte` (`/spaces/kontexte`) für persönliche und teamweite Wissens-, Brand-, Persona- und Kundenkontexte. Markdown-/Text-Import, Suche, Scope-Badges und ein klar markierter privater Arbeitskontext sind enthalten.
- **Verbindliche Skill-Quellen:** `skill_contexts` verknüpft Skills mit verpflichtenden Kontexten. Der Skill-Editor bietet Mehrfachauswahl; teamweite Skills akzeptieren ausschließlich teamweite Quellen. Backend lädt den aktuellen Inhalt deterministisch vor dem Workflow – bei Auto-Routing, `skill_read` und explizitem `/slash`. Fehlt dem Account ein Pflichtkontext, ist der Skill nicht ausführbar.
- **TickTick-Recherche repariert + Lemlist vorbereitet:** Der Research-Fehler war ein ungültiger freier JSON-Return, nicht eine fehlende TickTick-Schnittstelle. Blueprints werden jetzt über einen erzwungenen strukturierten Tool-Call erzeugt. Tristans bestehende TickTick-Anfrage lief beim Retry bis `review` durch (`https://mcp.ticktick.com/`, offizieller Bearer-Token-Fallback, `connect_ready=true`). MCP-Verbindungen unterstützen jetzt explizit keinen Header, Bearer und `X-API-Key`; damit sind TickTick und der offizielle Lemlist-MCP (`https://app.lemlist.com/mcp`) ohne eigenen OAuth-Client verbindbar. Neue MCP-Secrets werden AES-256-GCM-verschlüsselt gespeichert. TickTick wartet bewusst noch auf den vorhandenen Admin-Review-Klick und Tristans persönlichen API-Token.
- **Self-Reporting v1:** Administration → Nutzung zeigt Admins Zeitraumfilter (Woche, Monat, YTD, 12 Monate, seit Start), geschätzte Zeitersparnis, FTE-Tage, aktive Menschen, Contributions, Skill-Nutzung und Nutzung pro Person. Die Schätzung ist ausdrücklich als Näherung beschriftet und nennt ihre Formel (`3 Min./Antwort + 2 Min./erfolgreichem Tool-Call + 8 Min./Datei`) statt Scheingenauigkeit zu behaupten. `skill_usage_events` erfasst Auto-Routing, `/slash` und `skill_read`; bestehende `skill_read`-Calls werden rückwirkend übernommen. Contributions zählen erstellte Kontexte, Skills, Wiki-Seiten und Routinen.
- **Security & Verifikation:** Migrationen `20260717073201_context_foundation.sql`, `20260717075004_mcp_auth_schemes.sql` und `20260717075535_impact_reporting.sql` sind live auf Supabase und in der Migration History. Persönliche Kontexte haben bewusst keinen Admin-Bypass; neue Event-/Context-Tabellen besitzen restrictive `active_account_only`-Policies und explizite Grants. Echte temporäre Accounts bestätigten Multi-Abteilung, privaten Kontext, Cross-Account-RLS und Brand-Voice-vor-Workflow. Supabase Security Advisor ohne neue Findings; Performance-Hinweise für fehlende FK-Indizes behoben. Backend-Tests 11/11, JavaScript-Syntax und Desktop-/390×844-Browserprüfung grün. Production-Onboarding bestätigte Passwortwechsel, `finance + operations`, privaten Zielkontext und anschließende Produkttour; der Production-Admin-Test lud reale aggregierte Impact-Werte samt Methode. Railway-Deployment `a81c1cf2-d4dc-4607-98c4-5710d60e5330` ist `SUCCESS`; Netlify liefert Commit `920bfa9`. Temporäre Daten wurden vollständig entfernt.

### Session 2026-07-16 — Einladungen ohne Ablauf- oder Preview-Risiko

- **Neuer Standard:** `/api/invite` erzeugt bestätigte Supabase-Accounts mit einem kryptografisch zufälligen Startpasswort statt eines Invite-/Magiclinks. Das Passwort wird ausschließlich einmal in der Admin-Response gezeigt, nicht lesbar gespeichert und bleibt gültig, bis der Nutzer im Pflicht-Onboarding sein eigenes Passwort setzt.
- **Klare Admin-Übergabe:** Die Mitgliederverwaltung zeigt nach „Zugang erstellen“ eine kompakte Karte mit E-Mail und Startpasswort. „Login-Daten kopieren“ erstellt eine fertige Nachricht mit `https://os.enneo.ai`, E-Mail, Startpasswort und dem Hinweis zum persönlichen Passwort.
- **Pflicht-Onboarding vor Workspace:** Neue Accounts laden vor Name, Profilfoto, Abteilung und persönlichem Passwort weder Chats noch Pods, Tools oder Realtime-Abos. Erst nach erfolgreichem Passwortwechsel wird der Workspace initialisiert und die Produkttour gestartet. Bereits eingerichtete Accounts können über den Invite-Endpunkt nicht versehentlich zurückgesetzt werden.
- **Aktueller Team-Rollout:** Die alten Identitäten von `richard@enneo.ai`, `kyung@enneo.ai` und `tristan@enneo.ai` wurden nach Session-Revoke vollständig gelöscht und als neue bestätigte Admin-Accounts mit neuen UUIDs, leerem Onboarding-/Tour-Status und noch keinem Login erstellt. Individuelle Startpasswörter wurden separat an Aleksa ausgegeben und werden bewusst nie in Git, Handoff oder Audit-Metadaten gespeichert. Der zusätzliche Testaccount `aleksa@pengoro.com` wurde anschließend vollständig inklusive Auth-User, Profil, Sessions, Pending-Invite und Avatar-Prüfung gelöscht.
- **Produktiv verifiziert:** Tests 8/8, JavaScript-Syntax und Diff-Check grün. Railway-Deployment `600458e2-81b7-44c9-9598-322b0bc887a5` ist `SUCCESS`, `/health` grün und Netlify liefert Commit `55532e1`. Der echte Production-Eval bestätigt: Credentials statt Link, bestätigter Account, Pflicht-Onboarding, Login mit Startpasswort, Wechsel auf persönliches Passwort, Ablehnung des alten Startpassworts und erfolgreicher Login mit dem neuen Passwort. Alle Testaccounts, Profile und Pending-Invites wurden entfernt.

### Session 2026-07-15 — Slack kann Einladungen nicht mehr vorab verbrauchen

- **Root Cause bei Richard bestätigt:** Der rohe Supabase-Invite wurde nur acht Sekunden nach dem Erstellen bestätigt, lange vor Richards eigener Rückmeldung. Das passt zum Slack-Link-Preview-Crawler, der den GET-basierten Einmal-Link beim Erzeugen der Vorschau eingelöst hat. Richards Auth-User und Session wurden dadurch zwar erstellt, beim späteren menschlichen Klick war der Link aber bereits verbraucht.
- **Crawler-sicherer Link:** `/api/invite` gibt keine `supabase.co/auth/v1/verify`-URL mehr aus, sondern `https://os.enneo.ai/invite?token_hash=…&type=…`. Ein bloßer GET zeigt nur eine enneo-OS-Bestätigungskarte und verändert Auth nicht. Erst der bewusste Klick auf „Einladung annehmen“ bzw. „Sicher anmelden“ ruft `verifyOtp` auf.
- **Invite und Re-Invite abgedeckt:** Neue Accounts verwenden `type=invite`, bestehende Accounts wie Richard `type=magiclink`. Nach erfolgreicher Bestätigung wird die URL auf `/chat` bereinigt; neue Nutzer landen direkt im Profil-Onboarding. Ungültige Links zeigen eine verständliche Erneuerungsaufforderung statt still auf der normalen Seite zu landen.
- **Verifiziert und live:** Tests 8/8, Syntax und Diff-Check grün. Echter Production-Browser-Eval: Landing-GET ließ `confirmed_at` und `last_sign_in_at` leer; erst der Button-Klick bestätigte den Account und öffnete sichtbar das Onboarding. Der Magiclink-Pfad wurde zusätzlich mit einer echten temporären Session geprüft. Railway-Deployment `ebf2bf31-fa54-400b-845c-32d96fed4bde` ist erfolgreich, Netlify liefert Commit `1adc6d2`. Alle Testaccounts wurden gelöscht.

### Session 2026-07-15 — Slash-Skills sind keine Anfangskommandos mehr

- **Autocomplete an jeder Wortposition:** Ein `/` nach Leerraum öffnet die Skill-Auswahl nun auch mitten in einer bereits formulierten Nachricht. Auswahl und Einfügen ersetzen ausschließlich das aktuelle Slash-Fragment; der Text davor und danach bleibt erhalten.
- **Auswahl bleibt nach dem Senden sichtbar:** Bekannte, für den Nutzer verfügbare `/skill-slugs` werden in eigenen und fremden Team-Nachrichten als kompakte violette Skill-Labels gerendert. Alte Nachrichten erhalten die Darstellung beim erneuten Öffnen ebenfalls.
- **Echte Ausführung passend zur UI:** Backend und Pod-Frontend erkennen explizite Slash-Skills jetzt an jeder Wortposition. Dadurch ruft beispielsweise „Erstelle mit /dokument …“ Enni auch in einem Pod wirklich auf und lädt den gewählten Skill – nicht nur das Dropdown.
- **Verifiziert und live:** Backend-Tests 7/7, JavaScript-Syntax und Diff-Check grün. Der echte Browser-Test mit temporärem Account öffnete nach „Erstelle mit dem /“ mitten im Satz 26 Vorschläge, setzte `/dokument` als Composer-Label und behielt es nach dem Senden als violette Message-Pill (`rgba(149,116,255,.28)`, 7 px Radius). Railway-Deployment `d5b7aba4-d4c4-4924-a73d-e91504993392` ist erfolgreich; Netlify liefert Commit `dccf28b`. Testaccount und Testkonversation wurden anschließend vollständig entfernt.

### Session 2026-07-15 — Benachrichtigungen folgen dem echten Lesestatus

- **Sichtbarer Chat ist sofort gelesen:** Der neue autorisierte Endpunkt `POST /api/conversations/:id/read` löscht den Conversation-Unread-State und markiert passende Inbox-Benachrichtigungen gemeinsam als gelesen. Noch nicht versendete Pushes werden atomar übersprungen; fremde Nutzer erhalten für dieselbe Conversation einen 404.
- **Versteckter Tab meldet Ennis Antwort:** Bei einem SSE-Abschluss in einem nicht sichtbaren Tab erzeugt das Frontend zusätzlich eine lokale Service-Worker-Benachrichtigung. Sie verwendet denselben Message-basierten Tag wie der serverseitige Web Push, damit Browser keine doppelten Hinweise zeigen.
- **Browser-Tab zeigt offene Hinweise:** Der Dokumenttitel trägt bei offenen Meldungen jetzt z. B. `(2) enneo OS`; App-Badge und Titel werden aus demselben Unread-Zähler gespeist. Beim Zurückkehren in den bereits offenen Chat wird dessen Notification automatisch gelesen.
- **Push-Race geschlossen:** Der Push-Ticker lädt nur ungelesene Pending-Einträge und prüft direkt vor dem Versand nochmals den aktuellen `read_at`-/`push_state`-Stand. So löst eine inzwischen gelesene Antwort keinen verspäteten Push aus.
- **Verifiziert und live:** Backend-Tests 6/6, Syntax und Diff-Check grün. Lokaler und produktiver 2-Account-Eval bestehen jeweils alle 5 Checks (Outsider blockiert, Owner akzeptiert, Conversation gelesen, Notification gelesen, Pending Push übersprungen). Railway-Deployment `555ca078-93d7-41f1-95e4-906ba7feec98` ist erfolgreich; temporäre Accounts und Daten wurden entfernt.

### Session 2026-07-15 — Restricted heißt wirklich eingeladen

- **Kein Admin-Bypass mehr:** Restricted Pods sind jetzt auch für Account-Admins unsichtbar, solange sie nicht als Mitglied eingeladen wurden. `aleksa@spalevic-consulting.de` hatte im „Dello Projekt“ keinen Mitgliedseintrag; ausschließlich der frühere Admin-Bypass machte den Pod sichtbar. Die produktive Rollen-Simulation liefert jetzt exakt `0` sichtbare Dello-Pods.
- **Grenze durchgängig gehärtet:** Migration `20260715194247_restricted_pods_require_membership.sql` schützt Pods, Mitgliedslisten, Aufgaben/Kommentare, Konversationen, Attio-Verknüpfungen und den Storage-Bucket `pod-files` über dieselbe Sichtbarkeitsregel. Eingeladene Admins dürfen weiterhin Mitglieder und Pod-Einstellungen verwalten; nicht eingeladene Admins dürfen weder lesen noch schreiben.
- **Backend-Sonderweg entfernt:** `podIfVisible()` akzeptiert Restricted Pods nur noch für Ersteller und explizite Mitglieder. Damit umgehen die service-role-basierten Pod-/Attio-Endpunkte die Datenbankregel nicht mehr.
- **Produkttour ruhig und mittig:** Der Tour-Overlay verwendet keinen `backdrop-filter` mehr, nur noch eine leichte Abdunklung. Die Karte sitzt über `top: 50%` exakt in der vertikalen Mitte, bleibt auf kleinen Viewports scrollbar und erzeugt bei 390 × 844 keinen horizontalen Overflow.
- **Verifiziert und live:** Backend-Tests 6/6, Syntax, Diff-Check und Supabase Remote-Lint grün. Der echte 3-Account-DB-Eval besteht 11 Checks inklusive nicht eingeladenem Admin, Membership-Leak und blockiertem Storage-Upload; der Production-Attio-Eval besteht 12 Checks und bestätigt zusätzlich den Backend-404 für denselben Admin-Fall. Desktop-Abnahme bei 1280 × 720: Kartenmitte `360 px`, Mobile bei 390 × 844: `422 px`, jeweils `backdrop-filter: none`. Railway-Deployment `5af99d06-7098-4626-8e0d-2fef3d366925` ist erfolgreich, `/health` grün. Temporäre Testaccounts wurden gelöscht.

### Session 2026-07-15 — Invite-Links auf produktiver Domain

- **Eine kanonische Domain:** Supabase Auth `site_url`, Redirect-Allowlist, Railway `SITE_URL`, Railway `FRONTEND_ORIGIN` und der Backend-Fallback zeigen jetzt auf `https://os.enneo.ai`. Die Netlify-Domain bleibt nur als erlaubter technischer Fallback erhalten.
- **Alt-Testaccount vollständig entfernt:** `info@aleksa.ai` / „Aleks Test“ wurde inklusive Auth-User, Profil, Sessions, ausstehender Einladung und Avatar-Objekt gelöscht und kann neu eingeladen werden.
- **Produktiv verifiziert:** Railway-Deployment `7d0e1b7b-bc25-41fc-aad6-91065c708bda` ist erfolgreich und `/health` grün. Ein echter temporärer Invite erzeugte `redirect_to=https://os.enneo.ai`; der Testaccount wurde anschließend gelöscht.

### Session 2026-07-15 — People, Onboarding & verständliche Zugriffe

- **Verbindliches Invite-Onboarding:** Neue Profile durchlaufen serverseitig gespeichert drei Schritte für Name/Profilfoto, Abteilung und Passwort. Feste Abteilungen erhalten konsistente Farben; „Custom Tag“ erlaubt eigene Position und Farbe. Bestehende Accounts wurden bewusst als abgeschlossen migriert, damit nur neue Einladungen den Pflichtfluss sehen.
- **Kurze Produkttour:** Nach dem Onboarding erklärt eine 5-Schritt-Tour Chat, Pods, Tools/Spaces, Team-Verzeichnis und Benachrichtigungen. Abschluss bzw. Überspringen wird geräteübergreifend in `profiles.tour_completed_at` gespeichert.
- **Menschen überall sichtbar:** Neue Rail-Navigation „Team“ gruppiert aktive Accounts nach Abteilung und zeigt Foto, Name, Rolle und Label. Klickbare Profilansichten stehen dort, in Pod-Mitgliederlisten und an Team-Nachrichten zur Verfügung; eigene Chat-Nachrichten zeigen ebenfalls den Avatar.
- **Restricted Pods wirklich verwaltbar:** Pod-Besitzer und explizit eingeladene Admins können in den Einstellungen Mitglieder hinzufügen oder entfernen. Open Pods erklären den automatischen Teamzugriff; Members sehen die Liste und Profile read-only. Nicht eingeladene Admins erhalten keinen Sonderzugriff.
- **Marketplace entwirrt:** Tools, Skills und Routinen erklären jetzt einheitlich „Persönlich / Ausgewählt / Teamweit“ und tragen sichtbare Scope-Labels. Nicht konfigurierte OAuth-Tools zeigen Members „Admin erforderlich“ statt eines scheinbar funktionslosen „Einrichten“-Buttons; Klick erklärt den nächsten Schritt. Die Spaces-Sidebar trennt „Enni konfigurieren“ von „Wissen & Spaces“.
- **Datenmodell & Verifikation:** Migration `20260715175415_people_onboarding_and_departments.sql` ergänzt Abteilung, Custom-Label/-Farbe sowie Onboarding-/Tour-Zeitstempel mit expliziten Spalten-Grants; sie ist live und in der Remote-History synchron. Frischer temporärer Member wurde im Browser vollständig durch Onboarding, Tour, Team-Profil, Member-Marketplace und Restricted-Pod-Erstellung samt Hinzufügen eines Mitglieds geführt; Test-Pod und Testaccount danach gelöscht. Backend-Tests 6/6, JS-Syntax und Diff-Check grün.

### Session 2026-07-15 — Attio-Kundenidentität pro Pod

- **Eindeutiger Kunde statt Namensraten:** In den Pod-Einstellungen kann genau ein primäres Attio-Unternehmen verknüpft werden; Kontakte und Deals lassen sich optional ergänzen. Die Suche schlägt anhand des Pod-Namens passende Records vor und validiert jeden Beta-Suchtreffer über den aktuellen Record-GET, bevor ein Snapshot gespeichert wird.
- **Rollen sauber getrennt:** Nur Pod-Owner und Admins dürfen verknüpfen, ändern, synchronisieren oder lösen. Members sehen den Kunden und zugehörige Records read-only. Browser-Direktwrites sind durch Grants + RLS blockiert; Mutationen laufen ausschließlich über authentifizierte Backend-Endpunkte.
- **Enni kennt den Kunden, lädt aber nicht blind alles:** Der Pod-Systemkontext enthält Name, Domain und stabile Attio-Record-IDs. Einfache Identitätsfragen werden direkt beantwortet. Notizen, Meetings, Transkripte oder weitere CRM-Details lädt Enni nur bei tatsächlichem Bedarf über die bestehenden read-only Attio-Tools.
- **Datenmodell:** Migration `20260715171459_pod_attio_links.sql` führt `pod_attio_links` und `pod_attio_related_records` ein. Gespeichert werden Referenzen und kompakte Identity-Snapshots, nie die gesamte CRM-Historie. Migration ist live und in der Supabase Migration History.
- **Verifiziert:** Backend-Tests 5/5; Supabase Grants/RLS und Security Advisor sauber. Production-Eval `run-pod-attio-production.js` bestand 11 Checks inklusive echter STAWAG-Suche, Member-403, Outsider-Isolation, Sync und Enni-Antwort `STAWAG, stawag.de` ohne CRM-Tool-Call. Desktop und 390×844 sind visuell geprüft, ohne horizontalen Overflow. Railway-Deployment `c7d926cb-af70-4287-9f62-62656514e97d` ist erfolgreich und `/health` grün.

### Session 2026-07-15 — Enni-Projektstand-Aufruf und Slash-Skill-Labels

- **Projektstand ruft Enni jetzt wirklich:** „Enni · Projektstand analysieren“ setzt der automatisch erzeugten Pod-Nachricht nun `@enni` voran. Damit durchläuft sie bewusst denselben Mention-Guard wie manuell markierte Teamnachrichten und erzeugt eine Enni-Antwort statt nur eines still gespeicherten Pod-Updates.
- **Slash-Skills als Composer-Label:** Ein über das Slash-Menü ausgewählter, verfügbarer Skill wird im bestehenden Highlight-Layer jetzt genauso als kompakte violette Pill dargestellt wie `@enni` und Pod-Mitglieder. Der eigentliche Textwert bleibt unverändert (`/slug `), sodass Backend-Routing, Tastaturbearbeitung und Copy/Paste identisch weiterlaufen.
- **Verifiziert:** JavaScript-Syntax, Backend-Tests 4/4 und Diff-Check grün. Browser-Test bestätigte `/dokument` als gerendertes Skill-Label sowie die automatisch gesendete `@enni`-Projektstand-Nachricht inklusive Enni-Antwortcontainer.

### Session 2026-07-15 — Alternative Kanban-Ansicht für Pod-Aufgaben

- **Board als echte Alternativansicht:** Zwischen „Aufgaben“ und „Dateien“ liegt der neue URL-deeplinkbare Reiter `?tab=board`. Er zeigt dieselben `pod_tasks` in den vier Spalten Offen, In Arbeit, Blockiert und Erledigt — ohne zweite Datenhaltung oder abweichende Zustände.
- **Direktes Arbeiten:** Desktop-Nutzer verschieben Karten per Drag-and-drop. Für Touch, Tastatur und Screenreader hat jede Karte zusätzlich einen benannten Status-Selektor. Ein Klick auf die Karte öffnet dasselbe vollständige Aufgabendetail wie in der Liste; Realtime hält die sichtbare Board-Ansicht aktuell.
- **Ruhigere Übersicht:** Die große Leiste „Aktueller Fokus“ samt Status und Zieltermin wurde vollständig aus der Pod-Übersicht entfernt. Kennzahlen, Aufmerksamkeit, letzte Aktivität und Schnellaktionen bleiben erhalten; die zugrunde liegenden Projektfelder bleiben vorerst in den Einstellungen editierbar.
- **Responsive & zugänglich:** Das Board scrollt auf schmalen Viewports nur innerhalb seines eigenen Containers, Spalten rasten beim horizontalen Scrollen ein, Touch-Ziele sind 44 px groß, Fokuszustände und Live-Statusmeldungen sind vorhanden und Reduced Motion wird respektiert.
- **Verifiziert:** Backend-Tests 4/4, JavaScript-Syntax, Diff-Check und realer Browser-Test erfolgreich. Der visuelle Fixture-Test bestätigte die vier Spalten, URL-State, Detailöffnung und einen persistenten Statuswechsel zwischen Board-Spalten.

### Session 2026-07-15 — Pods als interaktives Team-Projektmanagement

- **Project Pulse als Startansicht:** Jeder Pod öffnet mit Kennzahlen für offen/in Arbeit/blockiert/überfällig, automatisch priorisierter „Benötigt Aufmerksamkeit“-Liste und einer kombinierten Aktivität aus Aufgaben, Konversationen und Dateien. Die Bereiche sind URL-deeplinkbare Tabs (`?tab=overview|convs|tasks|board|files|settings`). Die anfangs vorhandene große Fokus-/Status-/Zieltermin-Leiste wurde in der Folgesession wieder entfernt.
- **Aufgaben sind jetzt echte Arbeitsobjekte:** Migration `20260715155329_pod_project_pulse.sql` ergänzt Beschreibung, Priorität (`low/normal/high/urgent`) und Status `blocked`. Ein zugängliches Desktop-Sidepanel bzw. Mobile-Bottom-Sheet bietet Titel, Status, Priorität, Verantwortlichen, Termin, Abschnitt, Beschreibung, Konversationslink und Team-Kommentare. Fokus-Trap, Rücksprungfokus, Unsaved-Changes-Warnung, Reduced Motion und 44-px-Mobile-Ziele sind abgedeckt.
- **Team-Kommentare mit harter RLS:** Neue Tabelle `pod_task_comments`; sichtbare Pod-Mitglieder dürfen lesen, nur als eigene Identität schreiben, nur eigene Kommentare ändern/löschen (Admins dürfen löschen). Realtime hält geöffnete Pulse-/Task-Ansichten aktuell. Migration `20260715160158_pod_project_pulse_advisor_indexes.sql` ergänzt die vom Supabase Advisor geforderten FK-Indizes.
- **Bestehenden RLS-Fehler behoben:** Der echte 3-Account-Test fand eine Rekursion in `pod_members` beim Hinzufügen zu Restricted Pods. Migration `20260715160310_fix_pod_member_rls_recursion.sql` verschiebt die Owner/Admin-Prüfung in `private.can_manage_pod_members()`; der reproduzierbare Test bestätigt Owner-Erstellung, Teamzugriff, Outsider-Isolation, Anti-Spoofing, Kommentare und Status-Updates (7 Checks).
- **Enni als Projektmanager:** „Enni · Projektstand analysieren“ startet aus dem Pod eine kontextgebundene Analyse von Fortschritt, Blockern, Verantwortlichkeiten und nächsten Schritten, ohne ungefragt Änderungen vorzunehmen.
- **Verifiziert:** Backend-Tests 4/4, JS-Syntax und Diff sauber, Supabase Remote-Lint ohne Schemafehler, Migration History synchron. Desktop-Abnahme bei 1280 px und Mobile bei 390×844 ohne horizontalen Overflow; Task-Kommentar und Statuswechsel real gespeichert, Realtime aktualisierte die Kennzahlen, Browser-Konsole ohne Fehler. Evals: `backend/evals/run-pod-project-pulse.js` und `backend/evals/run-pod-pulse-visual-fixture.js`.

### Session 2026-07-15 — Developer Workspace als Coming-soon-Bereich

- **Zukunft sichtbar, Zugriff noch geschlossen:** Der Marketplace zeigt jetzt eine eigene Kategorie „Developer Workspace“ mit Claude Code und Codex als „Coming soon“. Beide Einträge beschreiben Code-, Test- und Deployment-Zugriff über ein künftiges privates Enneo-Admin-Plugin.
- **Klare Produktgrenze:** Der Bereich ist ausdrücklich mit „Nur Admins“ und „Private Zugänge für die interne Weiterentwicklung von enneo OS“ gekennzeichnet. Damit bleibt er visuell und konzeptionell vom bestehenden Kunden-/Partner-Plugin für Enneo-Instanzen getrennt.
- **Nur Visualisierung:** Die Zeilen sind bewusst nicht interaktiv; es wurden noch keine Credentials, Repository-Rechte oder internen Plugin-Endpunkte freigeschaltet. Desktop und 390-px-Mobile sind ohne horizontalen Overflow geprüft.

### Session 2026-07-15 — Rollenbasierter UX/UI-Engineering-Skill

- **Ein Skill, zwei technisch getrennte Modi:** `/ux-ui-engineering` ist teamweit sichtbar. Members dürfen Screenshots/Feedback mit Wiki + GitLab read-only untersuchen und ausschließlich eine eigene strukturierte Anfrage erstellen bzw. verfolgen. Admins erhalten zusätzlich Queue-Verwaltung und die privilegierten GitLab-Umsetzungswerkzeuge.
- **Harte Grenze statt Prompt-Versprechen:** Der Toolkatalog wird pro Account aus `profiles.is_admin + account_status` gebildet. Members sehen nur `ux_ui_request_change` und `ux_ui_list_my_requests`; Admins zusätzlich `ux_ui_list_requests`, `ux_ui_manage_request`, `gitlab_ui_create_branch`, `gitlab_ui_write_file` und `gitlab_ui_create_merge_request`. Jeder privilegierte Tool-Call prüft die Rolle erneut serverseitig.
- **Sicherer GitLab-Workflow:** Schreibzugriff gilt nur für den konfigurierten `enneo`-Namespace, nur bei einem zuvor freigegebenen Request und nur auf Branches mit Prefix `enni/ui-`. Default-Branch-Writes, Merge und Auto-Merge existieren nicht. Ein Request kann erst nach vorhandenem MR-Link plus dokumentierter Build-/CI-/Visual-Verifikation abgeschlossen werden.
- **Eigene Review-Queue:** Migrationen `20260715150844_ux_ui_change_governance.sql` + `20260715151854_ux_ui_active_account_guard.sql` sind live. `ui_change_requests` ist browserseitig read-only; Members sehen per RLS nur eigene Rows, aktive Admins alle. Deaktivierte Accounts sehen auch mit altem JWT nichts. Administration → Freigaben hat eine neue kompakte UX/UI-Queue mit Details, Admin-Notiz, Rückfrage, Ablehnen, Freigeben, Umsetzung und geprüftem Abschluss.
- **Verifiziert:** Unit/Policy-Tests 4/4; echte temporäre Member-/Member-/Admin-Accounts bestätigten Tool-Trennung, Cross-Account-RLS, gesperrte Browser-Writes, Disabled-JWT-Guard und Admin-Freigabe. Reale Sonnet-Läufe: Member erstellte selbstständig genau einen Request ohne Mutation; Admin las dieselbe Queue und respektierte „noch nichts ändern“. GitLab-Smoke auf `enneo/ops-fe`: temporärer `enni/ui-*`-Branch + Datei-Write + Read erfolgreich, anschließend Branch gelöscht.
- **Produktiv verifiziert:** Commit `b0880b3` ist auf `main`; Railway-Deployment `d8112863-0e90-49ea-9314-cbc4837ce513` ist erfolgreich und `/health` grün. Der echte Production-Smoke bestätigte Request-Erstellung, Member-Isolation, getrennte Tool-Kataloge, Member-403 auf Admin-PATCH und Admin-Freigabe. Frontend ist auf `os.enneo.ai` live; Desktop- und 390-px-Mobile-Abnahme zeigten die kompakte Queue samt Detail-/Entscheidungsansicht ohne horizontalen Overflow oder Browserfehler. Temporäre Accounts, Requests und GitLab-Branches wurden vollständig entfernt. Reproduzierbare Checks: `backend/evals/run-ux-ui-production.js` und `backend/evals/run-ux-ui-visual-fixture.js`.

### Session 2026-07-15 — Enni IQ, Persönlichkeit und bewusstes Tool-/Skill-Routing (live)

- **Senior-Persönlichkeit statt Assistenten-Floskeln:** Der System-Prompt verlangt Ergebnis/Empfehlung zuerst, konkrete belegte Aussagen, freundlichen Widerspruch bei falschen Annahmen und klare Unsicherheitsmarkierung. Generische Einleitungen, Frage-Wiederholungen und austauschbare Ratschläge sind explizit ausgeschlossen.
- **Verbindliches Entscheidungsprotokoll:** Vor jeder Antwort rekonstruiert Enni intern das echte Nutzerziel, wählt den spezifischsten Skill, plant die passende Evidenzquelle, recherchiert nur so weit wie nötig und synthetisiert daraus eine Konsequenz statt Tool-Ausgaben nachzuerzählen.
- **Live Capability Map pro Account/Turn:** Der Prompt enthält jetzt den tatsächlich verfügbaren Katalog für Wiki/Docs, GitLab-Monorepo, Live-Enneo-Instanzen, Attio, Slack, Outlook, Drive, Notion, Skills, Brand-Dateien, Pods und zusätzliche MCPs. Nicht verbundene Systeme werden als solche markiert; bei Bedarf greift der bestehende Connection-Request-Flow.
- **Skill-Komposition:** Spezial-Skills schlagen `/enneo-context`. Kombinierte Aufgaben werden in Reihenfolge geladen, z. B. erst `/sales-call-prep`, dann `/praesentation`. Dokumente und Decks müssen vor `create_file` den jeweiligen Brand-Skill laden.
- **Automatisches Skill-Routing:** Enni lädt passende Team-Skills bereits serverseitig anhand der Anfrage — u. a. `/api-frage`, `/sales-call-prep`, `/health-check`, `/dokument` und `/praesentation`; `/enneo-context` ergänzt Enneo-Fakten, ohne spezifischere Skills zu verdrängen. Content-Skill kommt vor Output-Skill.
- **Begrenzte Recherche statt Tool-Schleifen:** Suchwerkzeuge haben pro Turn sinnvolle Budgets. Wird das Iterationslimit nach einem Tool-Ergebnis erreicht, erzwingt das Backend einen letzten Synthese-Schritt, damit nie eine leere Antwort zurückkommt.
- **Regression und Real-Evals:** `npm test` 3/3 grün; vier echte Eval-Fälle bestanden: belegte Produktantwort, technische Frage mit Wiki + GitLab-Monorepo, WBS-Legal-Call-Prep mit Attio sowie eine 3-Folien-Präsentation mit `/praesentation` und Enneo-Brand. Syntax- und Diff-Prüfungen ebenfalls sauber.
- **Production-Smoke bestanden:** isolierter temporärer Account gegen `https://enneo-os-backend-production.up.railway.app/api/chat`; Live-Turn nutzte `wiki_search` + `wiki_semantic_search`, beantwortete Copilot/Copilot+/Autopilot direkt und quellenbasiert, SSE-Turn schloss fehlerfrei ab. `/health` meldet Chromium unter `/usr/bin/chromium`. Testaccount und Testkonversation wurden anschließend gelöscht.
- **Live-Stand:** Code ab `enneo-AI/os@24732f5`; Railway-Deployment `e637a2e4-0f87-4cd8-a58d-3705f08322ed` erfolgreich. Der reproduzierbare Produktionscheck liegt in `backend/evals/run-production-smoke.js`.

### Session 2026-07-15 — Enni Research Lab für fehlende Tools

- **Tool nur nennen oder verlinken:** Im Marketplace gibt es jetzt „Tool anfragen“. Name, Website/Dokumentation und ein optionaler gewünschter Use Case reichen; die Recherche läuft im Hintergrund und bleibt beim Verlassen der Seite aktiv.
- **Quellenbasierter Blueprint:** Enni sucht und liest offizielle Entwicklerquellen über Firecrawl und erstellt mit Sonnet einen strukturierten Integrationsentwurf: Auth-Typ, Remote-MCP/API/OAuth, Scopes, Funktionen, Zugriffsebene, Sicherheitsnotizen, Einrichtungsschritte, Evidenzlinks und Confidence. Webseiteninhalt wird ausdrücklich als untrusted data behandelt; Endpoints/Scopes dürfen nicht erfunden werden.
- **Freigabe statt Blindflug:** Entwürfe erscheinen im Admin-Bereich unter Freigaben → Neue Integrationen. Erst nach Admin-Review werden sie für alle im Marketplace sichtbar. Offiziell belegte Remote-MCPs ohne OAuth sind danach direkt über den bestehenden sicheren MCP-Flow verbindbar; OAuth/API-Anbieter zeigen den recherchierten Einrichtungsplan und das Anbieterportal, werden aber nicht fälschlich als ausführbar markiert, solange ein nativer Adapter fehlt.
- **Security & Persistenz:** Migration `20260715131020_autonomous_tool_research.sql` führt `tool_requests` mit RLS ein. Nutzer sehen eigene Anfragen plus veröffentlichte Integrationen; Admins sehen alle. Browser-Writes sind gesperrt, Erstellung/Recherche/Review laufen nur über authentifizierte Backend-Endpunkte. Private/localhost-URLs werden abgelehnt, Credentials sind nie Teil der Recherche.
- **Robustheit:** Status `queued → researching → review → approved/rejected` sowie `failed` mit Retry; ein Backend-Neustart nimmt liegengebliebene Jobs wieder auf. Die Marketplace-UI pollt nur während aktiver Recherchen.
- **E2E verifiziert:** Testanfrage „DeepWiki“ mit offizieller Devin-Dokumentation durchlief produktiv `queued → researching → review`; Enni identifizierte korrekt `https://mcp.deepwiki.com/mcp`, Typ `remote_mcp`, direkt verbindbar und acht Evidenzquellen. Testentwurf anschließend gelöscht. RLS-Test mit zwei echten temporären Accounts: Owner sah private+approved, anderer nur approved, direkter Browser-Insert → `403`; Testaccounts gelöscht.

### Session 2026-07-15 — Premium-Fokus und externe Accounts

- **Keine lila Input-Rahmen mehr:** Die globale violette Browser-Outline wurde für Inputs entfernt. Chat-Composer und Formfelder nutzen jetzt einen neutralen, theme-adaptiven Glass-Fokus aus feiner Kante und weichem Halo; die Tastatur-Navigation bleibt sichtbar. Der Composer hebt den gesamten Glass-Container hervor statt ein Rechteck um die innere Textarea zu zeichnen.
- **Beliebige E-Mail-Domains:** Admins können jetzt auch Testnutzer, Kunden und Partner außerhalb von `@enneo.ai` einladen. Backend und Datenbank-Trigger akzeptieren jede syntaktisch gültige E-Mail; Rollen werden weiterhin ausschließlich über `pending_invites` vergeben. Migration `20260715125703_allow_external_invite_emails.sql` ist live.
- **Verifiziert:** Ein temporärer `@example.com`-Auth-User wurde erfolgreich inklusive normalem Profil angelegt und danach wieder vollständig gelöscht. Die alte Domain-Sperre aus Migration `0009` ist damit bewusst durch die neue Migration überschrieben.

### Session 2026-07-15 — Globale und Restricted Routinen

- **Drei echte Zielgruppen:** Routinen können jetzt `Nur ich`, `Alle Accounts` oder `Ausgewählte Accounts` gelten. Nur Admins dürfen globale/restricted Routinen anlegen; Members bleiben persönlich und können ihre Routine wie bisher zur globalen Freigabe vorschlagen.
- **Account-genaue Ausführung:** Globale und restricted Routinen erzeugen pro aktivem Ziel-Account einen eigenen privaten Enni-Lauf. Dadurch werden die persönlichen Tools und Learnings des jeweiligen Accounts verwendet und die Kosten korrekt diesem Account zugeordnet. Pod-Routinen bleiben bewusst ein einzelner persönlicher Lauf, damit kein Ergebnis mehrfach im selben Pod erscheint.
- **UI:** Der Routine-Editor hat einen kompakten Zielgruppen-Switch und bei „Ausgewählte“ eine Account-Liste mit Avatar, Name und E-Mail. Listenzeilen zeigen die Zielgruppe; globale manuelle Testläufe verlangen vor dem accountübergreifenden Start eine Bestätigung. Mobile geprüft bei 390 px ohne horizontalen Überlauf.
- **Security:** Migration `20260715123308_routine_audiences.sql` ist live und in der Migration History. Neue Tabelle `routine_accounts` hat RLS und nur `SELECT` für `authenticated`; Writes laufen über den authentifizierten Backend-Endpoint. Echte JWT-RLS-Prüfung: zugewiesener Account sah global+restricted, nicht zugewiesener nur global, beide keine fremde persönliche Routine. Backend-Deployment `3d2f7020-014b-4949-b911-ee293ee11792` ist live.

### Session 2026-07-15 — Tool-Icons auf allen Routen repariert

- **Root Cause:** Markenbilder wurden relativ (`./icons/...`) referenziert und dadurch auf Deep Links wie `/spaces/skills` fälschlich unter `/spaces/icons/...` angefragt. Safari zeigte deshalb in bestehenden Skills und im Tool-Picker das Broken-Image-Symbol.
- **Fix:** Alle statischen und dynamischen Icon-Referenzen sind jetzt routenfest (`/icons/...`). Wiki nutzt Ennis Bot-Icon; GitLab, Attio, Outlook, Slack, Google Drive, Notion und die Enneo-Plattform zeigen ihre jeweiligen Markenbilder. Der generische Layer-Fallback bleibt ausschließlich für Tools ohne eigenes Markenasset.

### Session 2026-07-15 — Vollständige Smartphone-Optimierung

- **Mobile App-Shell:** Unter 900 px ersetzt eine kompakte Glass-Topbar die Desktop-Navigation. Die komplette Sidebar öffnet als zugänglicher Off-Canvas-Drawer mit Backdrop, Fokusführung, Escape-/Swipe-Schließen und dynamischem Seitentitel. Safe-Area-Insets decken iPhone-Notch und Home-Indikator ab.
- **Alle Kernbereiche responsive:** Chat, Marketplace, Spaces/Wiki, Pods (Konversationen, Aufgaben, Dateien, Einstellungen), Skills, Routinen, Admin und Kostenansichten wurden auf einspaltige, platzsparende Smartphone-Layouts umgestellt. Tabs scrollen horizontal; Tabellen und Code bleiben innerhalb ihres Bereichs scrollbar; Modals und Tool-Details erscheinen als bedienbare Bottom Sheets.
- **Chat für Touch:** Composer bleibt oberhalb der Safe Area, Modellwahl öffnet viewport-sicher, Textfelder nutzen 16 px gegen iOS-Autozoom. Prompt Queue sitzt kompakt am Composer und lässt sich jetzt auch per Touch-Drag umsortieren; zusätzlich gibt es „Nach oben“/„Nach unten“ als robuste Alternative.
- **Bedienbarkeit & Accessibility:** Primäre Touch-Ziele sind mindestens 44 px groß, Icon-Aktionen haben zugängliche Namen, Fokuszustände sind global sichtbar, Task-Aktionen verwenden echte Buttons und Reduced Motion wird respektiert. Light/Dark wurden auf 390×844 sowie 430×932 geprüft; Chat, Marketplace, Pod und Admin haben keinen horizontalen Seitenüberlauf.

### Session 2026-07-15 — Live-Diktat mit präziser Finalisierung

- **Text erscheint schon beim Sprechen:** MediaRecorder zeichnet weiterhin die hochwertige Audiospur für ElevenLabs Scribe auf; parallel schreibt die Browser-Spracherkennung Interim-Ergebnisse live in den aktiven Chat- oder Pod-Composer.
- **Hybrid statt Qualitätsverlust:** Nach dem Stop bleibt die Live-Vorschau sichtbar und der Status wechselt zu „Transkript wird präzisiert …“. Anschließend ersetzt das gemischte Deutsch-/Englisch-fähige Scribe-Ergebnis nur den diktierten Abschnitt; vorhandener Text vor dem Diktat bleibt erhalten. Bei einem Serverfehler bleibt die Live-Fassung stehen.
- **Klarer Aufnahmezustand:** kompakter LIVE-Status mit animierter Audio-Welle in der Input-Bar, roter Composer-Fokus und Stop-Quadrat im Mikrofon-Button; während der kurzen Finalisierung wird das Feld gegen versehentliche Änderungen gesperrt. Light/Dark, Reduced Motion und schmale Viewports sind abgedeckt.
- **Sicheres Senden:** Enter oder Senden während der Aufnahme stoppt zuerst das Diktat und wartet auf den Finaltext. Erst die nächste Send-Aktion verschickt die Nachricht; eine unfertige Interim-Fassung kann dadurch nicht versehentlich gesendet werden.

### Session 2026-07-15 — Prompt Queue als Composer-Shelf

- **Queue aus dem Chat-Verlauf gelöst:** Wartende Prompts erscheinen nicht mehr als gedimmte Chat-Bubbles, sondern in einem kompakten, an die Input-Bar angedockten Glass-Shelf. Dadurch bleibt der aktive Turn visuell oben und die Ausführungsreihenfolge direkt am Eingabepunkt verständlich.
- **Reihenfolge vollständig bedienbar:** Sechs-Punkt-Griff links für Drag-and-drop, zusätzlich Pfeiltasten am fokussierten Griff. Eine Drop-Linie zeigt die neue Position; „Steer“ verschiebt einen Prompt sofort an die erste Stelle.
- **Queue-Aktionen:** einzelne Prompts entfernen, alle entfernen sowie über das Mehr-Menü bearbeiten, duplizieren oder ans Ende stellen. Dateianhänge erscheinen platzsparend als kleiner Badge. Bei mehr Einträgen scrollt nur das Shelf, nicht der Chat.
- **Bestehendes Verhalten erhalten:** Queue bleibt pro Konversation isoliert, wird beim Chatwechsel korrekt ein-/ausgeblendet und nach Ende des laufenden Turns weiterhin automatisch von oben nach unten abgearbeitet. Light/Dark, Reduced Motion und schmale Viewports sind berücksichtigt. Reiner Frontend-Change; kein Railway-Deploy erforderlich.

### Session 2026-07-15 — OAuth-Integrationsplattform

- **Railway-freie Einrichtung:** Outlook, Google Drive, Notion, Attio und Slack verwenden eine gemeinsame OAuth-Plattform. Admins hinterlegen die OAuth-App einmalig direkt im Marketplace (Redirect URI, Client-ID, Client-Secret, optional Microsoft-Tenant); Secrets werden AES-256-GCM-verschlüsselt gespeichert und sind weder über die Data API noch in der UI auslesbar. Danach erfolgt jede Account-Verbindung per normalem Anbieter-Login.
- **Echte Connectoren statt Platzhalter:** Die Phase-2-Zeilen sind entfernt. Outlook erhält read-only Mail-/Kalender-Tools, Google Drive Suche/Lesen und Notion Suche/Seiten/Data Sources. Attio nutzt seine bestehenden sieben CRM-Tools jetzt ebenfalls per OAuth; Slack läuft über dieselbe generische Infrastruktur. Refresh-Tokens von Microsoft, Google und Notion werden serverseitig erneuert.
- **Zukunftsfähiger Provider-Flow:** Gemeinsame Endpoints `GET /api/oauth/providers`, `PUT /api/admin/oauth/providers/:provider`, `POST /api/oauth/:provider/start` und `GET /api/oauth/:provider/callback`; ein neuer Anbieter braucht nur Registry-Metadaten, OAuth-Austausch und Tool-Definitionen, keine neue Credential-UI.
- **Berechtigungen:** Admin-Verbindungen werden teamweit angelegt; Member-Verbindungen bleiben persönlich. Native Connectoren sind auch aus Ennis `request_tool_connection`-Karte direkt per OAuth verbindbar. Ein Admin kann erst verbinden, wenn die Anbieter-App einmalig konfiguriert ist; dies ist bewusst in enneo OS statt in Railway gelöst.
- **Migrationen:** `20260715091620_oauth_provider_platform.sql` + `20260715092145_lock_oauth_provider_secrets.sql`. Expliziter Test bestätigt: `authenticated` und `anon` haben keinerlei Tabellenrecht auf `oauth_provider_configs`, `service_role` allein hat Zugriff.

### Session 2026-07-15 — Account Governance + Enni Knowledge-/Release-Sync

- **Zwei Rollen konsequent umgesetzt:** Member arbeiten im eigenen Scope; Admins verwalten zusätzlich Team-Ressourcen und Freigaben. Invite-Flow nimmt `Member`/`Admin` entgegen und speichert die Rolle serverseitig über `pending_invites` statt unsicherer User-Metadaten. Mitgliederliste erlaubt Admins Rollenwechsel sowie Aktivieren/Deaktivieren; eigener Admin und letzter aktiver Admin sind geschützt.
- **Einheitlicher Scope erweitert:** Routinen und Wiki-Seiten nutzen jetzt ebenfalls `personal → proposed → team`, inklusive Admin-Review. Admins können persönliche Skills und Custom-Tools gezielt einem Account zuweisen. Team-Wiki-Seiten sind für Members read-only; neue Member-Seiten und URL-Imports starten persönlich.
- **RLS gehärtet:** persönliche Connector-Metadaten und Restricted-Space-Wiki-Inhalte sind nicht mehr org-weit sichtbar. Eine restrictive `active_account_only`-Policy liegt auf 25 Fachtabellen, sodass deaktivierte Accounts auch mit altem JWT keinen Data-API-Zugriff behalten. Rollen-/Freigabeaktionen laufen über Backend-Guards und werden in `audit_log` attribuiert.
- **Enni bleibt aktuell:** `knowledge_sources` enthält die offizielle vollständige Produktdokumentation (`docs.enneo.ai/llms-full.txt`) sowie gemergte Änderungen aus `enneo/enneo`. Der Backend-Ticker synchronisiert alle 6 Stunden, führt Hash-Deduplizierung und RAG-Reindexierung in 20-Seiten-Batches durch und protokolliert jeden Lauf. Admin-UI „Wissen & Releases“ zeigt Status und erlaubt manuellen Sync.
- **Wöchentlicher Digest:** Team-Routine „Wöchentliche Enneo Release Notes“ läuft montags 09:00. Release-Kontext der letzten 30 Tage wird kompakt und mit GitLab-Quellen in Ennis Systemkontext geladen. Initialer Live-Sync: 80 gemergte Änderungen, Dokumentationsindex 417 deutsche Seiten; erster breiter Batch 20 Seiten erfolgreich.
- **Migration:** `20260715084328_account_governance_release_sync.sql` atomar live auf Supabase `aiwhomrvspfxotkllngz`, Migration History eingetragen. Neue Backend-Module: `audit.js`, `knowledge-sync.js`.

### Session 2026-07-14 — UI/UX-System, Spaces und visueller Skill-Tool-Picker

- **Premium-Glass-System konsolidiert:** Ambient-Hintergründe für Light/Dark, stärkerer Blur für Sidebar/Composer und vollständiger sticky Modal-Footer bis zum unteren Rand. Neue Chats begrüßen personalisiert nach Tageszeit und Profilname.
- **Admin, Pods und Prompt-Queue entschlackt:** Admin ist in Freigaben/Nutzung/Mitglieder geteilt; Pods haben einen kompakten Header ohne redundante Mitglied-/Status-Texte, moderne Datei-Aktionen (Preview/Download), passende Upload-/Neue-Konversation-Aktionen, echte Profilbilder bei Aufgaben und verfeinerte Task-Controls. Gequeuete Prompts bleiben immer unter dem aktiven Turn statt darüber zu springen.
- **Spaces/Wiki als kompakter Workspace:** einheitlicher Header, Inhalte/Quellen-Tabs mit Zählern, zweispaltige Ordner, kompakte Seitenzeilen, klare Back-Navigation und konsistente Unterseiten/Editoren. Light und Dark visuell geprüft.
- **Skill-Tools ohne technische IDs:** neuer authentifizierter Endpoint `GET /api/tools/catalog` liefert pro Nutzer die statischen plus tatsächlich verbundenen MCP-/Attio-/Slack-Tools (nur Name/Beschreibung, keine Credentials/Schemas). Der Skill-Editor zeigt Connection-Icons und verständliche Aktionen, bietet Suche/Mehrfachauswahl und Entfernen per ×; die Roh-IDs bleiben ausschließlich im versteckten Datenfeld. Das Wiki nutzt dasselbe vergrößerte Enni-Bot-Asset wie der Marketplace. Bestehende `skills.tools[]` bleiben unverändert kompatibel.
- **Primäraktionen immer oben:** „Neuer Skill", „Tool verknüpfen" und „Neue Routine" stehen jetzt im Seitenkopf; die alten CTA-Zeilen am Listenende sind entfernt. Audit der übrigen Hinzufügen-Aktionen ergab keine weiteren Bottom-only-CTAs.
- **Live-Stand:** Frontend via Netlify-GitHub-Auto-Deploy; Backend wegen fehlender Railway-GitHub-Verknüpfung explizit aus `backend/` mit `railway up --detach` deployed. Railway-Deployment `be7198f0-2339-492d-9ee5-5c8d037ccd68` SUCCESS, `/health` 200, Tool-Katalog unauthentifiziert korrekt 401. Code-Stand: `519850f`.

### Session 2026-07-13 — Admin-Bereich neu strukturiert

- Die lange gemischte Admin-Seite wurde durch drei klar getrennte Arbeitsbereiche ersetzt: **Freigaben**, **Nutzung** und **Mitglieder**.
- Die drei Statistik-Karten und der funktionslose Modell-/Systembereich wurden entfernt. Nutzung erscheint als kompakte Zeilenansicht; Freigaben bündeln Wissens-, Learning-, Skill- und Tool-Vorschläge ohne wiederholte Erklärungstexte.
- Admin-Tabs synchronisieren ihren Zustand über `/admin?tab=reviews|usage|members`; Non-Admins landen automatisch auf Nutzung.
- Mitgliederverwaltung und Invite-Flow bleiben funktional, sind aber platzsparend in einer Zeile aufgebaut.

### Session 2026-07-09 — Wissens-Update-Loop (Admin-Review) + Skillstruktur

1. **Wissens-Update-Loop LIVE — als Admin-Review, NICHT als User-Learn-Card.** Aleksas Vorgabe: unerfahrene Nutzer dürfen Enni nicht schulen — Vorschläge werden gesammelt, NUR der Admin sieht und genehmigt sie (typisch 1×/Woche). Umbau der Vormittags-Version (die hatte User-Approve im Chat):
   - Migration `0012_knowledge_admin_only.sql`: RLS-select auf `knowledge_updates` nur noch für `is_admin`; alte `ku_review`-Policy (jeder Authentifizierte) entfernt. Updates laufen ausschließlich über Backend-Endpoints.
   - Backend: `/api/knowledge-update/:id/approve|reject` jetzt hinter `requireAdmin` (403 für Member — E2E verifiziert). Tool-Description + System-Prompt: Enni sagt dem Nutzer nur "als Wissens-Vorschlag notiert", verspricht nichts.
   - Frontend: Learn-Card aus dem Chat entfernt. Neues Admin-Panel **"Wissens-Updates"** (Admin-Bereich, Sidebar-Link, beides für Non-Admins hidden + RLS-gated): offene Vorschläge als Karten (Diff, Quelle "aus Konversation von X", Übernehmen/Ablehnen) + "Zuletzt entschieden"-Historie.
   - **E2E verifiziert:** Vorschlag → Admin-Panel → Übernehmen → Wiki-Seite aktualisiert + RAG-Chunks sofort re-embedded ("3 Chunks neu indexiert"); Non-Admin: 403 am Endpoint, leere Liste via RLS. Testdaten danach vollständig zurückgebaut (Seite, Chunks, Testuser).
2. **Skillstruktur v1 (Tristan-Roadmap Punkt 1).** Migration `0013_skills.sql`: Tabelle `skills` mit 6 Textblöcken — `context` (warum/wofür), `workflow` (wann macht man was), `tools[]` (verknüpfte Basis-Tools), `triggers` (Slash-Command + kontextbasiert), `definition_of_done`, `corner_cases` — plus `slug` (= künftiger Slash-Command), `enabled`. RLS: lesen alle, schreiben nur Admins. UI: neuer Bereich **Skills** unter Administration (Spaces-Sidebar, `/spaces/skills`), Zeilen-Liste im Tools-Look, breites Editor-Modal; Non-Admins sehen alles read-only (Editor disabled, kein Save/Delete — damit ist "Skill-Overview für Non-Admins" aus der Tristan-Liste ab Tag 1 drin). **E2E:** Skill angelegt → gelistet → bearbeitet geöffnet → gelöscht, RLS-Write als Non-Admin verweigert.
3. **Skillcalls LIVE (gleiche Session, nachmittags):** (a) `/`-Autocomplete im Composer + Pod-Startseite (gleiche Menü-Infrastruktur wie Mentions, Items generalisiert auf `insert`-Feld); (b) Trigger-Übersicht aller enabled Skills als zweiter System-Block pro Turn (`skillsPromptBlock`); (c) `skill_read`-Tool für kontextbasiertes Laden; (d) `/slug` am Nachrichtenanfang → Backend lädt vollen Skill als System-Block + Enni startet mit Workflow-Overview (in Pods zählt `/skill` auch ohne @enni als Enni-Aufruf). Quelle: `backend/src/tools/skills.js`. **E2E:** `/api-frage`-Call lieferte Overview + skill-konforme Antwort (Kurz-Antwort/Detail/Quelle). 5 Start-Skills sind angelegt.
6. **Echte PDF-Erstellung LIVE:** `create_file` rendert document/presentation jetzt standardmäßig als **echtes PDF** (format="html" nur auf Wunsch). Stack: `puppeteer-core` + Debian-Chromium auf Railway — ⚠️ Railway baut mit **Railpack**, NICHT Nixpacks (nixpacks.toml wird ignoriert!); Chromium kommt über die Service-Env-Var `RAILPACK_DEPLOY_APT_PACKAGES=chromium` (Pfad `/usr/bin/chromium`, `/health` zeigt ihn an). `src/pdf.js`: ein wiederverwendeter Browser pro Prozess, Dokument = A4 printBackground, Präsentation = 13.333×7.5in (PowerPoint-Maß) mit 1 Folie/Seite via injiziertem Print-CSS; `networkidle0` wartet auf Google Fonts. Fallback: schlägt PDF-Rendering fehl → HTML-Datei + ehrlicher Hinweis. Gotcha behoben: `execSync`-Suchkette braucht `; true` (Exit-Code des letzten Glob warf sonst, obwohl stdout den Pfad hatte). **E2E:** Chat-Auftrag → 2 echte PDFs (application/pdf, korrekte Seitenzahl), Erstseiten visuell geprüft. Known Trade-off: Folien-PDFs sind groß (~3 MB/Folie, rasterisierte Gradients).
8. **Learnings-Layer LIVE (Feedback-Button + „Lernen & Schließen"):** Migration `0018_learnings` — persönliche Learnings wirken SOFORT für den eigenen Account (System-Block pro Turn via `learningsPromptBlock`, Cap 2×2500 Zeichen, dedupliziert), Team-weit erst nach Admin-Freigabe (`share_status`: none/proposed/approved/rejected; rejected bleibt persönlich aktiv). Quellen: (a) **subtiler Feedback-Button** (Sprechblasen-Icon neben Copy, Hover) → Modal mit Text + Radio „Nur für mich" / „Für alle vorschlagen"; (b) **Chat-Schließen-Dialog** — ✕ im Chat-Header (nur bei offener Konversation) → Abbrechen / Chat schließen / **Lernen & Schließen** (`POST /api/conversations/:id/learn`: Haiku destilliert 0-3 Ein-Satz-Learnings aus dem Verlauf, sofort persönlich + proposed). Admin-Panel **„Learnings"** (nur Admin sichtbar): Vorschläge mit „Für alle übernehmen"/„Ablehnen" + Liste „Team-weit aktiv" mit Deaktivieren (`/api/learnings/:id/approve|reject|demote`, requireAdmin). **E2E komplett:** Feedback→persönlich aktiv (Antwort begann wie gelernt), Lernen&Schließen (ehrliches „nichts Lernbares"), Vorschlag→Approve→Team-weit→Deaktivieren. Design-Entscheidung: „Chat schließen" ist der explizite ✕-Button — Navigation/Konversationswechsel triggert den Dialog bewusst NICHT (wäre bei jedem Sidebar-Klick Lärm). Offen: Nutzer-Selbstverwaltung der eigenen Learnings (Profil-Ansicht) fehlt noch.
19. **Skills für alle + Skill-Creator + Diktat v2 (2026-07-13, Nacht):** (a) **Skills-Sharing-Modell** (Migration `0026`): `skills.category` + `skills.visibility` (team/personal/proposed — wie Learnings: personal/proposed wirken sofort NUR beim Ersteller, team-weit schaltet der Admin). RLS: jeder legt eigene personal/proposed-Skills an und bearbeitet sie; team-weit setzen kann nur der Admin; per SQL-Rollen-Simulation verifiziert (Fremde sehen persönliche Skills nicht). Backend filtert überall auf Sichtbarkeit: `loadEnabledSkills(userId)`, Slash-Call in index.js, `skill_read` (ctx), Frontend-Slash-Autocomplete. (b) **UI:** Skill-Liste gruppiert nach Kategorie (Meta ans Ende), Badges „Persönlich"/„Vorgeschlagen von X" (Admin sieht Ersteller), „Neuer Skill" für ALLE sichtbar; Editor mit Kategorie-Feld (Datalist), Sichtbarkeits-Select (team-Option für Member disabled) und **Aktiv-Schalter** (der fehlende Enable/Disable-Weg — feedback-review lässt sich jetzt per Klick aktivieren). Die 24 Bestands-Skills kategorisiert (Vertrieb/Marketing/Implementation/Kommunikation/Dokumente/Finanzen/Leadership/Produkt/Meta). (c) **`/skill-creator`** (team-weit, Meta): führt durch Zweck→Duplikat-Check→Entwurf→Bestätigung→`skill_create_draft` (neues Tool: legt personal Skill für den Nutzer an, mit Slug-Validierung + Konflikt-Meldung). E2E: Chat-Turn legte `e2e-testskill` als personal an. (d) **Diktat v2:** Mikro nimmt via MediaRecorder auf → `POST /api/transcribe` → **ElevenLabs Scribe scribe_v1 (versteht Deutsch+Englisch GEMISCHT)**. ⚠️ Braucht `ELEVENLABS_API_KEY` als Railway-Env — solange er fehlt, antwortet der Endpoint 503 und das Frontend fällt automatisch auf die Web-Speech-Erkennung zurück (localStorage-Flag `sttFallback`, zurücksetzbar). **Blocker entdeckt: der claude-team-Supabase-PAT (`~/.supabase/access-token`) ist abgelaufen** (401) — deshalb keine Bridge im claude-team-Projekt möglich; direkter Railway-Env-Weg gewählt. **Aleksa-Todos: (1) ✅ erledigt 2026-07-13 spät — `ELEVENLABS_API_KEY` gesetzt, Misch-Diktat E2E verifiziert (200 + language_code; Fallback-Flag dazu von localStorage auf in-memory umgestellt, damit Alt-Nutzer nicht in der Browser-Erkennung festhängen). (2) ✅ gelöst 2026-07-13 spät: Die Datei `~/.supabase/access-token` enthielt einen widerrufenen Wert — der gültige aleksaai-PAT stand in claude-teams `ai-team/status/STATUS.md` (Clairmont-Abschnitt) und liegt jetzt in der Datei; claude-team-Deploys funktionieren wieder (33 Functions sichtbar).**

23. **Marketplace + Modal-UX + Prompt-Warteschlange (2026-07-13, Nacht V — Aleksa-Feedback):** (a) **Marketplace statt Connections/Tools:** die zwei Seiten (v-conn „Tools" + v-admin-conn „Connections") waren künstlich getrennt (gleiche Dienste doppelt) → EINE Seite `v-marketplace` (`/spaces/marketplace`) mit Gruppen **Read & Write** (Enneo, Wiki, Outlook P2, WhatsApp/Telegram soon), **Read-only** (GitLab, Attio, Slack, Drive/Notion P2) und **Eigene Tools** (alle MCP-Rows in EINEM Container `dyn-tools`, `dyn-connections` entfällt; category-Feld bleibt in DB, steuert aber keine Anzeige mehr). Sidebar: ein Eintrag „Marketplace". (b) **Spaces hat keine Startseite mehr:** Rail-Klick auf Spaces + Route `/spaces` landen direkt im Marketplace (kein gemischtes Space-Home als Einstieg; Space-Übersicht weiter via Klick auf einen Space im Baum). ⚠️ Dabei gelernt: mehrere Keys im `views`-Mapping auf DIESELBE Section-ID brechen das Toggle (letzter Key gewinnt) — Aliase entfernt. (c) **Modal-UX:** `.modal{max-height;overflow-y:auto}` + `.m-actions` sticky bottom (full-bleed via negativen Margins, glass-Backdrop-Blur + Schatten + Hairline — Buttons IMMER sichtbar, vom Inhalt getrennt); globaler mousedown-Listener: Klick auf den Overlay-Hintergrund schließt jedes Modal (mousedown statt click, damit Text-Selektion im Modal nicht schließt). (d) **Prompt-Warteschlange:** sendet der Nutzer, während Enni arbeitet (eigener Stream ODER conv.working), wird die Nachricht pro Konversation gequeued (`promptQueues`): gedimmte User-Bubble + „In der Warteschlange · entfernen"-Tag (Klick entfernt), nach Turn-Ende `drainPromptQueue` → automatischer Send der nächsten; bei Konversations-Reopen werden Queue-Bubbles neu aufgebaut + gedrained. Enter queued (Send-Button bleibt Stop). **E2E:** Marketplace als Einstieg mit 3 Gruppen; Skill-Modal: sticky-Buttons im Viewport + outside-click schließt; Queue: 2. Nachricht während Haiku-Turn → Bubble+Tag → nach Antwort 1 automatisch gesendet → Antwort 2 korrekt. Test-Konv bereinigt. Kein Backend-Deploy nötig (nur Frontend).

22. **Per-User-Tools + Chat-Registrierungs-Karte + Workflow-Diagramm (2026-07-13, Nacht IV — Aleksas Playground-Vision, einheitlich umgesetzt):** Ordnungs-Prinzip: Workflows leben NUR in Skills (Text = Source of Truth, Diagramm = generierte Ansicht), Tools leben NUR unter Spaces → Tools (Chat-Karte ist nur ein Eingabeweg). (a) **Per-User-Tools:** Migration `0027` (`connectors.owner` + `visibility` personal/proposed/team; Bestand bleibt team). Tool-Loader attio/slack/mcp lösen per ctx.userId auf: eigener persönlicher Connector hat Vorrang vor Team (Map-Caches pro User); `POST /api/connectors` für alle (Non-Admin → immer personal, Admin default team, Re-Connect ersetzt im jeweiligen Scope); `POST /:id/share` (Owner → proposed), `POST /:id/(approve|reject)` (Admin; approve ersetzt bei attio/slack den alten Team-Connector), DELETE für Owner/Admin. Connections-Seite: Scope-Badges, „Teilen"-Button, native Zeilen für jeden verbindbar; **Admin-Panel „Tool-Vorschläge"** (wie Skill-Vorschläge). ⚠️ Fix dabei: conn/admin-conn-Views luden Connectors nur beim App-Boot — jetzt bei jedem Öffnen. (b) **Tool-Registrierungs-Karte im Chat:** neues Enni-Tool `request_tool_connection` (tools/registration.js, kind attio/slack/mcp) + System-Prompt-Regel „FEHLENDES TOOL → SOFORT Karte zeigen, nicht nach Credentials fragen" (Haiku fragte sonst erst nach — nachgeschärft). Frontend `renderConnectCards` (an renderWriteCards-Callsites): Karte mit Name/URL/Key-Feldern (Key = password-Input, geblurred), Sicherheits-Hinweis, POST mit scope personal, Erfolg → „✓ Verbunden". Enni sieht Credentials nie. (c) **Workflow-Diagramm:** `parseWorkflowSteps` (nummerierte Schritte) + `renderWorkflowFlow` (Nodes mit Verbindungslinie + Tool-Chips bei wörtlicher Tool-Nennung) + Text/Diagramm-Toggle im Skill-Editor; skill-creator-Skill um verbindliche Format-Konvention ergänzt (nummerierte Schritte, Tools im Schritt benennen — dadurch bleibt ALLES einheitlich visualisierbar). **E2E:** Sharing-Zyklus komplett (personal→Teilen→proposed-Badge→Admin-Panel→approve→team in DB); Enni-Turn „Linear anbinden" → Karte im Chat mit Feldern + Fehlerpfad (invalid MCP-URL → saubere Meldung, Button re-enabled); sales-call-prep-Diagramm 7 Nodes. Entscheidung dokumentiert: KEIN n8n-Canvas-Nachbau (agentisch ≠ deterministisch; echte Pipelines → n8n/Make via MCP anbinden). Testdaten bereinigt.

21. **Skill-Review-Panel + globale Suche (⌘K) + Skill-Suchfeld (2026-07-13, Nacht III — Review-Punkte 3-5):** (a) **Skill-Vorschläge-Panel** im Admin-Bereich (Sidebar-Link „Skill-Vorschläge", nur Admin, RLS-gated): listet `visibility='proposed'`-Skills mit Zähler „N offen" + Buttons Ansehen/Ablehnen/Für-alle-freischalten. Backend `POST /api/skills/:id/:action(approve|reject|demote)` (requireAdmin): approve→visibility 'team', reject/demote→'personal'. In alle Admin-Routing-Pfade eingehängt. (b) **Globale Suche (⌘K):** Command-Palette (`#pal-overlay`) — Sidebar-Button „Suchen ⌘K" + Shortcut ⌘/Strg+K (Toggle). Durchsucht Konversationen (`ilike` auf title, RLS-scoped = eigene + sichtbare Pod-Konvs — schließt die Lücke „private Konvs nicht durchsuchbar"), Pods, Wiki-Seiten (Cache `wikiPages`), Skills (`allSkills()` = team + eigene). Gruppiert, Pfeiltasten/Enter/Esc, Klick öffnet das Ziel (Konv/Pod/Wiki-Seite/Skill-Editor). 130ms-Debounce + `palSeq`-Guard gegen veraltete Antworten. (c) **Skill-Suchfeld** über der Skill-Liste: `loadSkills` cached jetzt (`skillListCache`), `renderSkillList(filter)` filtert clientseitig über Name/Slug/Kategorie und rendert die Kategorie-Gruppen neu. **E2E:** ⌘K öffnet, „pricing" → Wiki+Skill-Treffer, „Hosting"+Enter öffnet die Konversation; Skill-Suche „sales" → nur Vertriebs-Skills; Test-Vorschlag im Panel → freischalten → visibility=team in DB. Testdaten (Vorschlag-Skill + 3 alte Test-Learnings aus früheren Sessions) bereinigt.

20. **Invite-Flow + Space-Rechte im Tool-Layer (2026-07-13, Nacht II — Review-Punkte 1+2):** (a) **Einladen:** `POST /api/invite` (requireAdmin, nur @enneo.ai) erzeugt via `auth.admin.generateLink` einen Invite-Link (bei Bestandsnutzern automatisch Magic-Login-Link, `existing:true`) — bewusst Link-zum-Weitergeben statt E-Mail-Versand (Supabase-SMTP-Limits; Admin schickt eh persönlich). UI im Admin-Bereich → Mitglieder: E-Mail-Feld + „Link erzeugen" + Kopieren-Card; Mitglieder-Liste zeigt jetzt auch role_title. ⚠️ Supabase-Auth-Config gefixt: `site_url` stand noch auf localhost:3000 → jetzt `https://enneo-os.netlify.app` + uri_allow_list (localhost:5173/5180 für Previews) — vorher wären ALLE Auth-Links ins Leere gelaufen. (b) **Onboarding-Nudge:** Erst-Login mit leerem Profil (role_title+about) öffnet einmalig das Profil-Modal mit Willkommens-Hinweis (localStorage `onboarded-{uid}`). (c) **Space-Rechte im Tool-Layer:** `allowedSpaceIds(userId)` in tools/wiki.js (Admin = kein Filter; sonst open Spaces + eigene Restricted-Mitgliedschaften/-Erstellungen) — greift in wiki_list_pages, wiki_read_page, wiki_search (`.in('space_id',…)`), wiki_semantic_search (24 Kandidaten holen → auf sichtbare Slugs filtern → 8) und wiki_propose_update (unsichtbare Restricted-Seite = „existiert nicht"). Verwaiste NULL-space_id-Seite Company Data zugeordnet. **E2E mit echtem Test-User (Non-Admin, kein Space-Mitglied):** wiki_read_page auf Restricted-Seite → „existiert nicht"; semantische Suche nach deren Inhalt → kein Leak; Admin liest dieselbe Seite normal. Invite-E2E: neuer User → Invite-Link mit korrektem Redirect, Bestandsuser → Login-Link, fremde Domain → 400. Test-User + Test-Konvs gelöscht.

**Produkt-Review (Lücken für echtes Team-Arbeiten, Stand 2026-07-13 Nacht, priorisiert):**
1. ~~Invite-/Onboarding-Flow~~ ✅ erledigt (Punkt 20) — offen bleibt: Rollen-Modell (Admin/Builder/Member) + Microsoft-SSO.
2. ~~Space-Rechte im Tool-Layer~~ ✅ erledigt (Punkt 20) — GitLab-Tools bleiben bewusst org-weit (ein Repo-Zugang für alle).
3. ~~Skill-Vorschläge-Review-Panel~~ ✅ erledigt (Punkt 21).
4. ~~Globale Suche (Cmd-K)~~ ✅ erledigt (Punkt 21).
5. ~~Skill-Suchfeld~~ ✅ erledigt (Punkt 21).
6. **Wiki-Versionierung** — Seiten-Edits überschreiben ohne Historie/Undo; riskant, sobald mehrere Leute schreiben.
7. **Dateien-Tab ohne Vorschau/Download-Historie**; **Composer-Anhänge** landen nicht im Pod-Dateien-Tab (bewusst? klären).
8. **Mobile**: Sidebar/Pod-Tabs sind auf schmalen Screens nur rudimentär nutzbar.

18. **Team-Readiness-Paket (2026-07-13, spät III — Aleksas Production-Ready-Feedback):** (a) **Aufgaben:** Toggle „Erledigte anzeigen" im Kopf (localStorage `tasksShowDone`; Zähler zählen weiter alle) + **Zuweisen an Pod-Mitglieder** (Hover-Personen-Icon oder Avatar-Klick → `.assign-menu`-Popover mit „Niemand" + Kandidaten [open Pod = alle Profile, restricted = Mitglieder+Ersteller]); ▶-Start überschreibt eine explizite Zuweisung nicht mehr (setzt Starter nur bei assignee=null); `pod_list_tasks` nennt Zuweisung. (b) **Pod-Composer production-ready:** die dünne Quick-Zeile ersetzt durch den vollen Chat-Composer UNTEN im Konversationen-Tab (`.composer`-Optik, Anhänge via `podPendingFiles` → Übergabe an `pendingFiles`+send(), Diktat via generalisiertem `startDictation(btn, textarea)`, Shift+Enter Zeilenumbruch). (c) **Mention-Tags:** `.hl-wrap`/`.hl-back`-Backdrop hinter BEIDEN Composern (Chat + Pod) rendert Text transparent + lila Pills hinter @Enni/@Namen (Namen aus profilesCache, längste zuerst; Sync bei input/scroll/mentionPick) — Textarea hat `background:none`, deshalb pixelgenau. (d) **Learnings-UX:** kein `window.alert` mehr — „Lernen & Schließen"-Ergebnis erscheint IM Modal (Häkchen-Liste + Hinweis „Gespeichert unter Profil → Meine Learnings; Admin entscheidet team-weit"); neues Profil-Panel **„Meine Learnings"** (eigene Einträge mit Badges Persönlich/Team-weit vorgeschlagen/Team-weit aktiv + Löschen via RLS). (e) **Account-Personalisierung:** Migration `0025` (`profiles.role_title`, `about`), Profil-Felder „Rolle im Unternehmen" + „Über mich — womit soll Enni helfen?", Backend baut daraus pro Turn einen System-Block („Dein Gegenüber …"). **E2E:** Composer + @enni-Pill visuell, Zuweisung + Toggle, Profil speichert, Enni beantwortete „meine Rolle?" ohne Tools korrekt aus dem Personalisierungs-Block, Lernen&Schließen-Ergebnis im Modal; Testdaten bereinigt. Learnings-Architektur unverändert: Tabelle `learnings` (0018), persönlich = sofort aktiv nur im eigenen Account, team-weit erst nach Admin-Freigabe — Learnings sind bewusst NICHT im Wiki/Open Space.
17. **Tristans Kontext-Bibliothek + Skills importiert (2026-07-13, spät — Auftrag Tristan via Aleksa):** (a) **29 Wiki-Seiten** in Company Data (Open Space, alle Accounts) unter dem neuen Ordner **`kontext/`**: 27 Docs aus Tristans Zip (Company-Profile, Pricing-Model Stand Juni 2026, USPs, Vertical-Playbook Energie, MEDDPICC, Personas ×4, Rollen ×2, CRM-Data-Model/-Etiquette, Partner-Provisionen, Accounting/Tax, Brand-Voice, LinkedIn-Templates, SEO/GEO, Newsletter, Release-Notes, Pyramid-Principle, RFI/RFP-Guidelines …) + `kontext/uebersicht` (Index aus README, Datei-Refs → Slugs übersetzt) + `kontext/brand-tonality-guardrails` (separates v2-Dokument, Website-Texte). **Dedupe-Check:** kein Overlap mit Bestand (product-docs/api-docs/marketing-site sind Doku-Crawls, das hier ist internes GTM-/Firmenwissen); brand-voice (allgemein) und tonality-guardrails (Website) ergänzen sich — beide behalten. Alle 29 Seiten re-embedded (RAG). (b) **17 Skills** in die skills-Tabelle importiert (Mapping: Purpose→context, Inputs+Instructions+Context→workflow, Output format→definition_of_done, Edge cases+Related→corner_cases, description→triggers; Datei-Pfade `../../context/x.md` → Wiki-Slug `kontext/x`, PR-/CLAUDE.md-Mechanik → wiki_propose_update; Attio-Skills mit attio_-Tools verdrahtet; beehiiv/notion/github-Referenzen mit Ehrlichkeits-Hinweis, da nicht verbunden). `enneo-context`-Meta-Skill-Workflow komplett auf Wiki-Mechanik umgeschrieben. **`feedback-review` deaktiviert** (GitHub-Issue/PR-Loop, in enneo OS nicht ausführbar — Begründung in corner_cases). **E2E:** Persona-Frage → wiki_semantic_search fand die neuen Seiten, korrekte Antwort; `/pricing-consultant`-Slash-Call → Workflow-Overview + wiki_read_page kontext/pricing-model + Tier-Einschätzung. Import-Gotcha: Supabase Management API blockt python-urllib per Cloudflare (403) — User-Agent `curl/8.4.0` setzen.
16. **Sidebar-Highlight-Fix + Pod-Logos (2026-07-13, spät):** (a) **Bugfix:** Beim Wechsel von einem Pod in einen 1:1-Chat blieb der Pod in der Sidebar markiert — und `loadPods()` (Realtime) hätte ihn sogar wieder markiert, weil das Highlight an `activePod` hing (überlebt als Seiten-Zustand). Jetzt eigene Quelle `sidebarPodId` + `paintPodHighlight()`: folgt der offenen Ansicht (openPod → Pod, openConversation → deren pod_id oder null, newConversation → convPod oder null); loadPods rendert daraus. (b) **Pod-Logo:** Migration `0023` (`pods.logo_url` + Storage-Policies `pod-%` im avatars-Bucket). Einstellungen-Tab hat oben eine Logo-Sektion (Vorschau-Kachel, „Logo wählen" PNG/JPG/SVG max 2 MB, „Entfernen"); Upload/Cleanup beim Speichern (Prefix `pod-{id}-`, alte Dateien werden entfernt). Sidebar-Kachel zeigt das Logo (object-fit cover), Initialen bleiben Fallback. (c) **Gefundener Alt-Bug, gefixt in Migration `0024`:** der avatars-Bucket hatte KEINE select-Policy → `storage.list()`/`remove()` liefen für Clients leise ins Leere — auch der Profil-Avatar-Cleanup aus 0009 hat also nie alte Dateien gelöscht. Jetzt `avatars_select_authenticated`; verwaiste Test-Datei manuell entfernt. **E2E:** Pod→1:1-Wechsel löscht Markierung (auch nach Sidebar-Re-Render), Logo-Upload → Kachel zeigt Bild, Entfernen → Initialen zurück + Storage leer.
15. **Codex-inspirierte Agent-Chat-UX (2026-07-13, Abend — 4 Features, alle LIVE):** (a) **Stop-Button:** Send-Button wird zum ⏹, sobald in der offenen Konversation ein Turn läuft (eigener Stream ODER `working` von extern). `POST /api/conversations/:id/stop` (Auth: Owner/Pod-sichtbar) → In-Memory-Map `activeTurns` (convId→AbortController, 1 Prozess pro Railway-Service reicht) → Signal in `runEnniTurn` (ctx.signal): bricht den Anthropic-Stream ab (2. Arg `{signal}`), checkt vor jeder Iteration + vor jedem Tool-Exec; Teil-Text der Iteration wird Teil-Antwort, sonst Content-Fallback `_Gestoppt._`; Panel zeigt „⏹ Gestoppt". Endpoint räumt auch verwaiste working-Flags auf (Restart-Fall, `running:false`). (b) **Arbeitsdauer:** Migration `0022` (`messages.duration_ms`); live „Enni denkt nach … · 21s" (1s-Interval im Shimmer-Status), nach done + nach Reload „Gedanken · N Tool-Aufrufe · 54s" (`fmtDur`). (c) **Datei-Karten:** `renderFileCards()` ersetzt `a[href*="/files?u="]`-Links (create_file-Ausgaben) durch `.file-card` (Icon, Name, Ext, „Öffnen"; ersetzt den ganzen `<p>` wenn Link allein steht) — greift live bei done, beim DB-Reload und im Realtime-Insert-Pfad. (d) **Abschluss-Bullets:** System-Prompt-Regel — bei Arbeitsaufträgen 3-5 Bullets (getan / bewusst nicht getan / wie geprüft), bei einfachen Fragen keine. **E2E:** Turn gestartet → Stop bei 8s → „⏹ Gestoppt", DB working=false + duration_ms=7588 + content `_Gestoppt._`; CSV via create_file → Karte live + nach Reload; Timer live/final/reload verifiziert. ⚠️ Deploy-Race real erlebt: erster Stop-Test lief noch gegen den alten Container (Chromium-Build dauert Minuten) — vor Backend-E2E `railway deployment list` auf SUCCESS des neuen Deploys warten, /health reicht NICHT. Test-Konversationen danach gelöscht.
14. **Chat-Sidebar-Hierarchie + Pod-Aufgaben nach awork-Muster (2026-07-13, Nachmittag):** (a) **Sidebar:** Pods klar von privaten Chats getrennt — Pod-Zeilen tragen eine Initial-Kachel (`.pod-tile`, 21px, Ink-Gradient, lila wenn aktiv), Hairline-Divider zwischen Pod-Block und Sektion **„Chats mit Enni"** (umbenannt von „Konversationen"). **Aggregierter Status-Punkt pro Pod:** Lila-Puls wenn irgendeine Pod-Konv arbeitet, Grün wenn irgendwo Ungelesenes liegt (Query über alle sichtbaren Pod-Konvs in loadPods, live via `loadPodsDebounced()` in onConvChange, erlischt beim Öffnen der Konv). Active-Toggle über `dataset.pod` statt fragilem Namens-Vergleich. (b) **Aufgaben-Tab = awork-Struktur:** Migration `0021` (`pod_tasks.section` text default '', `due_date` date). Einklappbare Abschnitte (Chevron, Collapse-Zustand in localStorage `tsecCollapsed`), Zähler `✓ erledigt/gesamt` (grün bei voll), Abschnitte entstehen durch Benutzung wie Wiki-Ordner ('' = „Allgemein"; „＋ Neuer Abschnitt" legt Draft an, existiert ab erster Aufgabe); Hover-Aktionen am Abschnitt: umbenennen (bulk-update section) + auflösen (Aufgaben → Allgemein). Ghost-Zeile „○ Aufgabe hinzufügen" pro Abschnitt → Inline-Eingabe, **Enter speichert optimistisch** (neue Zeile wird lokal eingehängt, Fokus bleibt — WICHTIG: kein loadPodTasks nach Insert, sonst gehen Tastenanschläge beim schnellen Nacheinander-Anlegen verloren; genau der Bug wurde im E2E gefunden und gefixt). Task-Zeilen: Checkbox, in_progress = lila Puls-Punkt vor Titel, done = strikethrough + ans Abschnittsende, rechts Fällig-Datum (Mono, rot wenn überfällig; setzen via Hover-Kalender-Icon → verstecktes date-input + showPicker), Assignee-Initial-Avatar, ▶ Enni-Start, Hover-Löschen. Alte globale „Aufgabe hinzufügen"-Leiste entfernt. (c) Backend `pod_list_tasks` liefert nach Abschnitt gruppiert inkl. due_date (railway-deployed). **E2E (Preview + Headless-Login):** Abschnitt angelegt → 2 Aufgaben rapid nacheinander → Zähler live 0/3, Abhaken → 1/3 + Sortierung, Collapse/Reopen + localStorage-Persistenz, überfälliges Datum rot, Pod-Grün-Punkt via Realtime <2s, Dark Mode geprüft. Demo-Abschnitt „Konzept & Design" mit 3 Aufgaben im STAWAG-Pod stehen gelassen. ⚠️ Browser-Automation-Gotcha: `computer key "Return"` erreicht die keydown-Handler nicht — immer `"Enter"` senden.
13. **Ordner-System + Aktionen oben (2026-07-13, 2. Feedback-Runde):** (a) „＋ Neue Seite" (btn dark) + „Von URL importieren" (btn quiet) sitzen jetzt als `page-actions` im Seitenkopf von Space-Übersicht UND Gruppen-Listen — nichts mehr unterm Listenende verstecken (Aleksas Regel: Aktionen immer oben). (b) **Ordner generisch:** Gruppierung = erstes Slug-Segment (`groupPages` via Map statt fester PAGE_GROUPS-Liste; bekannte Crawl-Prefixe behalten sprechende Labels über `KNOWN_GROUPS`, unbekannte Prefixe werden hübsch kapitalisiert). Editor + Import-Modal haben ein **Ordner-Feld** (Datalist mit bestehenden Ordnern des Space via `spaceFolderPrefixes`, freie Eingabe legt neuen Ordner an; vorbefüllt mit dem Ordner, aus dem heraus geklickt wurde; bei Bestandsseiten fix, weil Slug stabil bleibt). Slug = `{ordner}/{titel}`. Backend import-url akzeptiert `folder` (Default „import"). Damit haben Restricted Spaces dieselbe Struktur wie Company Data — selbst aufgebaut. **E2E:** Seite mit neuem Ordner „Prozesse" im Restricted Space angelegt → Ordner erschien sofort in Übersicht + Sidebar-Baum; Buttons above-the-fold verifiziert. Demo-Inhalte im „Aleksa Test"-Space stehen gelassen.
12. **Spaces-Feinschliff + URL-Import (2026-07-13, nach Aleksa-Feedback):** (a) Lock-Bug gefixt (LOCK_SVG ist nur für Sidebar gestylt — in `sh-title` rendert jetzt ein inline-gestyltes 18px-Mini-Lock; vorher füllte ein schwarzes Riesen-SVG den Restricted-Space-Screen). (b) „+ Neue Seite" und „+ Von URL importieren" auch in jeder Gruppen-Seitenliste (pl-new-page/pl-import-url), nicht nur auf der Space-Übersicht. (c) **URL-Import LIVE:** `POST /api/wiki/import-url {url, space_id}` → Firecrawl-Bridge im claude-team-Supabase (`FIRECRAWL_BRIDGE_URL` + `FIRECRAWL_BRIDGE_TOKEN` [= claude-team Anon-Key] als Railway-Env) crawlt die Seite → Markdown mit `> Quelle: {url}`-Header → Upsert auf Slug `import/{titel}` → Auto-Reindex. **E2E:** docs.enneo.ai/de/welcome in den Restricted-Space „Aleksa Test" importiert — richtige Space-Zuordnung, 3 Chunks indexiert (Demo-Seite bewusst stehen gelassen). ⚠️ Firecrawl-Rate-Limit ~3 req/min (Aleksas Plan) — Einzelseiten ok, Bulk-Import bräuchte Pacing. (d) Gruppen-Labels ehrlich benannt: „Enneo Produkt-Doku · importiert aus docs.enneo.ai", „Enneo API-Referenz", „enneo.ai Website", neue Gruppe „Importierte Seiten" (`import/`-Prefix).
11. **Spaces-Redesign + Wiki-Editor (2026-07-13):** Spaces-Seite von Dust-Jargon auf klare Struktur. (a) **Sidebar:** EINE Sektion „Spaces" statt Open/Restricted-Doppel (Lock-Icon am Space, ein ＋; Modal behält den Open/Restricted-Schalter); Zwischenebenen „Connected Data/Folders/Websites/Tools" ersatzlos raus — der Baum zeigt direkt die Inhaltsgruppen mit Zählern (`PAGE_GROUPS` nach Slug-Prefix: Unternehmen & eigene Seiten, Produkt-Doku, API-Referenz, Enneo-API-Rezepte, Marketing-Website, „Weitere" als Fallback). Klick auf Space-Namen öffnet die Übersicht, Chevron klappt nur auf/zu. (b) **Space-Übersicht** (`v-space-home`, Default-Landung beim Öffnen des Spaces-Bereichs): Live-Suche über alle Seiten (Titel+Slug), Inhaltsgruppen, „Zuletzt geändert" (Top 6), Quellen (ehem. Connected Data) kompakt + Verwalten-Button. (c) **Seiten-Editor** (`v-page-edit`): „+ Neue Seite" (Titel → Auto-Slug inkl. Umlaut-Mapping, Unique-Konflikt sauber gemeldet) + „Bearbeiten"-Button auf jeder Seitenansicht; Markdown-Textarea mit Vorschau-Toggle; Löschen admin-only. (d) **Auto-Reindex:** neuer Endpoint `POST /api/wiki/reindex {slug}` (jeder Authentifizierte; nutzt reindexPage aus tools/wiki.js) nach jedem Speichern — **E2E bewiesen:** Seite über UI angelegt → Enni fand das Codewort sofort via wiki_semantic_search. `POST /api/wiki/delete {slug}` (admin-only) löscht Seite + Chunks (wiki_pages hat keine DELETE-RLS-Policy — Löschung läuft bewusst nur über Backend/service_role). Seiten-Cache (`wikiPages`) wird nach Schreibvorgängen invalidiert.
10. **Premium-Streaming-UX (2026-07-13):** Chat-Erlebnis von „AI-Slop" auf Produkt-Niveau. (a) **Gedanken/Antwort sauber getrennt an der Wurzel:** agent.js sammelt Text pro Tool-Loop-Iteration separat (`iterText`) — Zwischentext VOR einem Tool-Call ist Arbeits-Narrativ und wird ins `thinking` gemergt (`narrative[]`), nur die letzte Iteration ist `finalText`. Damit ist auch der aus der DB neu geladene `content` sauber (kein „Ich lese die Seiten…" mehr in der Antwort). (b) **Gedanken-Panel wie Claude:** startet zugeklappt, Header-Status **shimmert** live („Enni denkt nach…" / „Nutzt wiki_read_page…" / „Schreibt…“) — man sieht Aktivität auch zugeklappt; nach done statischer Status „Gedanken · N Tool-Aufrufe". (c) **Streaming-Cursor** (`.scursor`) am Antwortende während des Schreibens, verschwindet bei done. (d) **Scroll-Freiheit:** `followIfNearBottom()` (Auto-Scroll nur wenn <160px vom Boden) statt hartem `scrollTo` — Hochscrollen während des Streamens wird nicht mehr überschrieben. (e) **Live-Wiedereinstieg:** neuer Progress-Broadcaster (`backend/src/progress.js`, Supabase-Realtime-Broadcast `progress-{convId}`, 700ms-Snapshots, Payload gekappt) spiegelt Gedanken/Tools/Text; `renderLiveProgressIfWorking()` zeigt beim Öffnen einer arbeitenden Konv den Live-Fortschritt statt Leere-bis-fertig. Frisch-Check gegen DB entfernt Geister-Container bei Race. **E2E (Preview):** Panel zugeklappt+Shimmer beim Start, Narrativ landet im Panel / Antwort bleibt sauber (live UND nach DB-Reload), Cursor korrekt, kein Geister-Shimmer nach Fertigstellung. **Bekannte kleine Grenze:** `progress.take` cleart `state.text` bei tool_use — der Live-Reentry-Text zeigt nur den Text nach dem letzten Tool (konsistent mit finaler Antwort).
9. **Supabase Realtime LIVE (2026-07-12):** Migration `0020` (`alter publication supabase_realtime add table conversations, messages` — WALRUS respektiert die RLS-Policies, jeder Client sieht nur eigene + pod-sichtbare Rows). Frontend: (a) **Status-Channel** `conv-status` (INSERT+UPDATE auf conversations, ungefiltert = RLS-gescoped) → Sidebar-Punkte instant, Auto-Reload der offenen Konv bei externem Fertig-Werden, Pod-Konv-Liste live wenn Pod-Seite offen; (b) **Message-Channel** pro offener Konversation (`msgs-{id}`, INSERT auf messages) → **Pod-Team-Chat live**: fremde User-/Enni-Nachrichten erscheinen ohne Reload (eigene Nachrichten + eigene Streams rendern weiter lokal, activeStreams-Guard gegen Doppel-Render); Subscribe in openConversation, Unsubscribe in newConversation. Der alte 5s-Poll ist jetzt 60s-**Fallback** für abgerissene Sockets/Schlaf-Modus. **E2E:** externer unread-Update → grüner Punkt in <2s; Service-Role-INSERT in offene Konversation → Nachricht erschien live in <2s. Damit ist die offene Priorität „Realtime für Pod-Team-Chat" erledigt.
8. **Multi-Sessions + Sidebar-Status LIVE (2026-07-12):** Nutzer können eine Prompt senden, wegnavigieren und parallel weitere Konversationen führen — wie Claude Code/Codex. Migration `0019` (`conversations.working` + `unread`). **Backend:** atomarer Working-Lock pro Konversation (`UPDATE … WHERE working=false RETURNING` — zweiter Send in dieselbe Konv bekommt Fehler-Event statt Doppel-Turn), `working=false, unread=true` nach jedem Turn (auch Fehlerpfad räumt working auf), `emit` disconnect-sicher (try/catch um res.write — Turn läuft nach Browser-Schließen weiter und persistiert), Routinen setzen dieselben Flags (Cron-Ergebnis = grüner Punkt). **Frontend:** globale `streaming`-Sperre ersetzt durch `activeStreams`-Set + `sendingViews` pro Ansicht (`viewSeq`-Guards — Hintergrund-Streams fassen die neue Ansicht nicht an; DOM-Refs sind nach Wechsel detached = harmlos), Sidebar-Indikator `.c-ind` (Lila-Puls 2,4s = arbeitet, Grün = fertig-ungelesen; auch in Pod-Konv-Liste), 5s-Status-Poll (nur bei sichtbarem Tab; lädt offene Konv neu, wenn sie extern fertig wurde — Routine/anderes Gerät), unread wird beim Öffnen bzw. Live-Zuschauen gelöscht, Rückkehr in laufende Konv → nach Fertigstellung Auto-Reload mit finaler Antwort. **E2E (Preview):** Send→Wegnavigieren→Puls-Punkt, Parallel-Send in zweiter Konv, Grün-Punkt nach Hintergrund-Fertigstellung, Öffnen löscht Punkt + zeigt volle Antwort. Bekannte Grenze: Poll statt Realtime (5s Latenz auf Fremd-Events) — Realtime bleibt offene Priorität.
7. **Slack-Lese-Connector per Knopfdruck LIVE:** Migration `0017` (kind 'slack'). `backend/src/tools/slack.js` — 3 read-only Tools: slack_list_channels (public+private wo Bot Mitglied, paginiert), slack_read_channel (Auto-Join bei not_in_channel für öffentliche Channels, oldest_days-Filter, User-IDs zu Namen aufgelöst via users.list-Cache 10 Min), slack_read_thread. Verbinden: Slack-Zeile → Modal → Bot-Token (xoxb), Validierung via auth.test. Frontend: Attio-Code zu generischem `NATIVE_CONNECTORS`-Renderer verallgemeinert (Config-Objekt pro Dienst — nächste native Connectors sind nur noch ein Eintrag + Modal). **Voraussetzung für echten Connect:** einmalig Slack-App im enneo-Workspace anlegen (api.slack.com/apps; Scopes channels:read/history/join, groups:read/history, users:read) — kann Workspace-Admin-Approval brauchen. **E2E:** UI-Flow + Fehlerpfad (invalid_auth) verifiziert. **Ausblick besprochen:** Slack-Bot als Antwort-Kanal (Enni ersetzt Support-Bot) = eigener Ausbauschritt (Events API/Socket Mode + Webhook-Route ins Backend); WhatsApp/Telegram-Steuerung analog (Telegram trivial via Bot-API-Webhook, WhatsApp via Meta WABA).
6. **Attio-Connector per Knopfdruck LIVE:** Migration `0016` (`connectors.kind` 'mcp'|'attio' + Spalten-Grant — ⚠️ die 0010-Grant-Liste ist abschließend, neue Spalten brauchen explizites `grant select`). Nativer read-only-Connector `backend/src/tools/attio.js` (5 Tools: list_objects, query_records [Attio-Filter-JSON], get_record, list_notes, raw_get [nur GET, nur /v2]); Tools erscheinen pro Turn NUR wenn ein Attio-Row in `connectors` existiert. Verbinden: Attio-Zeile auf der Connections-Seite (Admin) → Modal → API-Key → `POST /api/connectors {kind:'attio', token}` validiert gegen `/v2/self`, Re-Connect ersetzt den alten Key; Key write-only (Spalten-Grant). Trennen über ✕. mcp.js lädt nur noch `kind='mcp'` (sonst MCP-Handshake gegen api.attio.com). **E2E:** UI-Flow + Fehlerpfad (ungültiger Key → Attio-Fehler im Modal) verifiziert; echter Connect braucht Aleksas Attio-Key (Workspace Settings → Developers → Access Token, Lese-Scopes).
5. **Routinen/Scheduler LIVE (Tristan-Roadmap Punkt 6):** Migration `0015_routines` (name, prompt, 5-Feld-cron [Europe/Berlin] + schedule_label, pod_id [null=privat], model [Default Haiku], enabled, last_run_at/last_result; RLS: eigene + Admin alle). **Ticker** läuft im Railway-Backend (`src/routines.js`, 30s-Intervall, pro Minute einmal, Berlin-Zeit via Intl, Minimal-Cron-Matcher für die UI-Presets, Doppel-Start-Schutz). Jeder Lauf = neue Konversation (privat oder im Pod) mit Prompt als User-Message + Enni-Antwort; System-Hinweis "keine Rückfragen, in sich geschlossenes Ergebnis"; Kosten in `llm_usage` mit `source='routine'`. UI: Bereich **Routinen** unter Administration (`/spaces/routinen`), Presets Täglich/Werktags/Wöchentlich/Monatlich + Uhrzeit, Ziel Privat/Pod, Modellwahl, ▶ "Jetzt ausführen" (`POST /api/routines/:id/run`, Owner/Admin). **E2E:** Routine über UI angelegt → run-now → Konversation mit korrekter Antwort (492 Wiki-Seiten) + Usage-Eintrag source=routine. ⚠️ Ticker-Grenze: läuft nur solange der Railway-Container lebt — bei Restart in der fälligen Minute wird ein Lauf übersprungen (kein Catch-up). Für Team-kritische Routinen später pg_cron-Fallback erwägen.
4. **Datei-/Präsentations-Erstellung im enneo-Brand LIVE:** neues Tool `create_file` (`backend/src/tools/files.js`) — kind `document` (A4-artig, druckbar, Inter/Navy/Purple, KPI-Cards, Purple-Blockquote), `presentation` (16:9-Deck, Ink-Navy + grainy Lavender-Gradient [SVG-feTurbulence], Geist/Geist Mono, Pfeiltasten-Navigation, Print-CSS) oder `raw` (CSV/MD/JSON/TXT). Brand-Templates: `backend/src/brand/enneo-brand.js` + bereinigte Logo-SVGs (⚠️ Original-SVGs aus dem Design-System-Ordner haben KEINEN Style-Block — st-Klassen-Fills mussten injiziert werden, sonst schwarze Box). Ablage: privater Bucket `generated-files` (Migration 0014), Signed-URL 7 Tage. ⚠️ Supabase Storage serviert HTML als text/plain (Anti-XSS) → Auslieferung über Backend-Endpoint `GET /files?u=<signed-url>` (nur eigener Bucket-Prefix erlaubt), dann rendert es im Browser + ist als PDF druckbar. **E2E:** Präsi über echten Chat-Turn erstellt, 4 Folien, Brand-Merkmale verifiziert, Screenshots geprüft. Design-System-Quelle: `~/Desktop/Enneo Design System/` (Style Guide v2026-01.3).

**Phase-2-Roadmap (Tristan-Liste, sortiert 2026-07-09):** 1) Skillstruktur ✓ → 2) Skillcalls (Slash + Kontext-Trigger + Workflow-Overview) → 3) Non-Admin-Overview ✓ (Teil von 1) → 4) Department-Briefings (Content, parallel) → 5) Post-to-Slack (als UI-Connection per Knopfdruck, OAuth-Flow für Connector-Layer nötig — bisher nur Bearer-Token-MCPs) → 6) Cronjobs/Routinen → 7) Missing Skills (Job Role Creator, Skill Creator Metaskill, OS Health Audit; Feedback-Audit erst nach Privacy-Entscheidung — Konflikt mit "Inhalte werden nie ausgewertet") → 8) Agent-to-Agent Orchestration.

### Session 2026-07-06 → 2026-07-08 — Was neu ist (Details in den Punkten 10c–10m unten)

Reine Ausbau-/Polish-Session auf dem live MVP. Reihenfolge wie gebaut:
1. **Design-Declutter** (10c): Emoji-Icons raus (Sidebar/Pods/Tabs/Badges/Tool-Rows), Lock-SVG nur für Restricted, Stroke-Icons statt Emoji auf Tool-Cards. → Aleksas Design-Regel: **keine Emoji-UI-Icons, keine funktionslosen Badges, keine Boxen um Listen/Empty-States** (gilt für alle künftigen Builds).
2. **Enneo-Plattform-Connector read-only** (10b): 4 Tools gegen die Mind-API (`https://{instanz}/api/mind`), universeller Production-Token auf Railway (`ENNEO_TOKEN`), Instanz per Name referenziert.
3. **Enneo-Writes mit Freigabe-Gate** (10d): `enneo_propose_write` → Freigabe-Karte im Chat → Approve/Reject-Endpoints → echte Ausführung, Audit-Tabelle `enneo_write_proposals` (Migration 0007). DELETE gesperrt. Instanz-Auflösung universell ({name}.enneo.ai).
4. **Enni-IQ-Upgrade** (10e): 15 Plugin-Skills als Wiki-Rezepte unter Slug `enneo-api/{thema}` importiert (43 RAG-Chunks), Prompt-Regel "Rezept lesen statt Endpoints raten". Fixte den Telefonnummern-Chat, wo Enni Endpoints riet.
5. **Darstellungs-Paket** (10f): Streaming-Absätze (kein Klebe-Text zwischen Tool-Runden), Haiku-Auto-Titel (analysiert 1. Nachricht, 1–5 Wörter), Sidebar-Zeitgruppen + Rename/Delete, **Dark Mode** (Semantik-Variablen + Toggle), Loading-Skeletons, Tool-Output-Clip 20k.
6. **Pod verlassen / löschen** (10g, Migration 0008): Verlassen für Mitglieder, Löschen nur Ersteller/Admin (RLS-enforced).
7. **Code-Copy + Profil-Bearbeitung + @enneo.ai-Zwang** (10h, Migration 0009): Copy-Button auf Code-Blöcken, Profil-Modal (Name/Avatar/Passwort), `handle_new_user`-Trigger erzwingt @enneo.ai-Domain, Bucket `avatars`.
8. **Antwort-Copy** (10i): subtiler Copy-Button in der Meta-Zeile jeder Enni-Antwort (nur Antwort-Text, nicht Gedanken).
9. **Modellauswahl** (10j): **Fable 5** (`claude-fable-5`, $10/$50) ergänzt, **Opus 4.8 Default bei jedem Chat-Start** (Reset in newConversation), Auto-Titel-Prompt auf 1–5 Wörter + Haiku umgestellt.
10. **enneo-Logo als Favicon + Tool-Logo** (10k): lila SVG `frontend/icons/enneo-icon.svg` (von enneo.ai/framerusercontent gezogen, auf `#7B5AE2` umgefärbt). Favicon + Logo auf der Enneo-Connection-Zeile.
11. **Echte Tool-/Connection-Verknüpfung via MCP** (10l, Migration 0010): Tabelle `connectors`, jeder MCP-Server per URL (+Token write-only), Kategorie tool/connection, live-Discovery im Tool-Loop (`src/tools/mcp.js`).
12. **Pods als Team-Chat nach Dust-Muster** (10m): Enni antwortet nur bei @enni, sonst reine Team-Nachricht; volle Pod-Kontext-Tools (`src/tools/pod.js`); Pod-Startseite mit Direkt-Input + Suche; Mention-Highlighting.

**Theme-Toggle-Position:** direkt neben dem Abmelde-Icon im Sidebar-Footer (2 Icons als Paar).

### Offene Prioritäten für die NÄCHSTE Session (Reihenfolge = Empfehlung)

1. [x] **Wissens-Update-Loop** — LIVE seit 2026-07-09, als Admin-Review (siehe Session-Block oben). Enni schlägt vor → Admin-Panel "Wissens-Updates" → Übernehmen aktualisiert Wiki + re-embedded Chunks sofort.
1b. [ ] **Skillcalls** — nächster Schritt der Tristan-Roadmap: `/slug` im Composer (Autocomplete aus `skills`), Trigger-Blöcke kompakt in Ennis System-Prompt, `skill_read`-Tool, Workflow-Overview bei Slash-Call.
2. [x] **Realtime für Pod-Team-Chat** — LIVE seit 2026-07-12 (siehe Session-Block oben, Punkt 9).
3. [x] **Mention-Autocomplete** — LIVE seit 2026-07-09: @-Dropdown (Composer + Pod-Startseite) mit @enni + Pod-Mitgliedern (Restricted: Mitglieder+Ersteller, Open: ganzes Team), Pfeiltasten/Enter/Tab/Esc, Document-Capture-Keydown verhindert Fehl-Send. Außerdem 5 Start-Skills nach dem neuen Muster angelegt (kickoff-vorbereitung, health-check, api-frage, stakeholder-email, voicebot-anpassung).
4. [ ] **Space-Rechte im Tool-Layer** — Enni ignoriert Restricted-Spaces beim Antworten noch (wiki/gitlab nicht nach User-Spaces gefiltert). Vertraulichkeit vor Team-Rollout.
5. [ ] **Eval-Harness** — Golden-Set von 15–20 echten Fragen mit erwarteten Kern-Fakten, per Script durchjagen. Fängt Enni-Regressionen bei Prompt-/Tool-Umbauten (wie beim Telefonnummern-Chat, der nur zufällig auffiel).
6. [x] **Routinen/Scheduler** — LIVE seit 2026-07-09 (siehe Session-Block oben, Punkt 5).
7. [ ] **RAG-Re-Index automatisieren** — aktuell manuell; DB-Trigger/Cron der geänderte Wiki-Seiten re-embedded.
8. [ ] **Report-Visualisierung** — Enneo-Reports (Telefonie/Ticket-Metriken) als Charts statt Texttabellen.
9. [ ] Aleksa-Todos: eigene Domain für die Netlify-Seite; Passwort-Änderung/Invite-Flow vor Team-Start (Profil-Modal kann Passwort schon, aber kein Invite).
10. [ ] **Zukunfts-Request (Aleksa 2026-07-13): Lokaler Ordner-Zugriff via Helper.** Enni soll in ausgewählten lokalen Ordnern auf dem Rechner des Nutzers arbeiten können (Ordner wählen + zustimmen). Entscheidung: NICHT über die Browser File System Access API (nur Chromium, Tab muss offen bleiben), sondern richtig — kleines lokales Helper-Programm pro Mitarbeiter (wie Codex/Claude Code), das sich per WebSocket mit dem Backend verbindet und Datei-Tools lokal ausführt. Security-Konzept nötig (Ordner-Allowlist, Consent-Flow, Audit). Bewusst zurückgestellt, erst nach den Kanal-Prioritäten (Telegram/Slack).

**Wartung nicht vergessen:** Wenn enneo das Claude-Code-Plugin updated → die 15 `enneo-api/`-Rezeptseiten neu importieren + Chunks re-embedden (Pattern in Punkt 10e). Backend deployt NUR via `railway up` (nicht git push); Frontend via git push (Netlify-Auto-Deploy).

### Was wurde in dieser Session gemacht (2026-07-03 → 2026-07-05)

Komplettes MVP von null auf live (Details in den nummerierten Punkten unten): DB-Schema + RLS → Backend (Anthropic-SDK-Tool-Loop, Railway via CLI) → Wiki-Seeding (303 Seiten aus Ennis KB) → GitLab-Connector (Keychain-PAT) → Frontend nach mockup-v5 → Dust-IA (expandierende Icon-Nav, kontextuelle Sidebar, Spaces mit echtem DB-Modell) → Kosten-Optimierung (pgvector-RAG 1.303 Chunks, Prompt-Caching, Modell-Dropdown; 0,23 € → 0,04 €/Frage) → Context-Compaction (Ring, 33/70/80) → Composer-Redesign (Anhänge Excel/CSV/Bild/PDF, Diktat DE/EN) → **Pods** (geteilte Konversationen, Aufgaben mit ▶-Enni-Start, Dateien, Instructions for Agents) → SPA-Routing + Netlify-GitHub-Auto-Deploy. Neo-Roboter aus ops-fe als Enni-Avatar. Login-User `aleksa@enneo.ai` angelegt (Passwort NICHT im Repo — hat Aleksa). Einstieg für neue Sessions: `CLAUDE.md`.

## Erledigt

- [x] Konzept + Dust.tt-Research (claude-team: `ai-team/agents/enni-enneo/knowledge/company-os-dust-research.md` + knowledge.md §89)
- [x] Klickbares Design-Mockup v5, von Aleksa final validiert (`design/mockup-v5.html`)
- [x] Konzept-Pivot: EIN Agent (Enni) statt Agenten-Zoo; Wiki als Notion-Ersatz; Slack-Abgrenzung
- [x] Supabase-Projekt `enneo OS` angelegt (aiwhomrvspfxotkllngz, Frankfurt)
- [x] GitHub-Repo `enneo-AI/os` + lokaler Clone `~/Desktop/Projects/enneo-os` + Auth eingerichtet
- [x] Repo-Grundgerüst (README, CLAUDE.md, docs, design)
- [x] **DB-Schema live** (2026-07-03): `supabase/migrations/0001_mvp_schema.sql` via Management API eingespielt — `profiles` (auto-angelegt bei Auth-Signup, `is_admin`-Flag), `conversations`, `messages` (inkl. `thinking` + `tool_calls` jsonb fürs Gedankenketten-Panel), `llm_usage` (Token + `cost_eur` pro Antwort, source chat/routine), `wiki_pages` (Markdown), `knowledge_updates` (Diff + proposed/approved/rejected). RLS auf allen 6 Tabellen: Conversations/Messages owner-only, Wiki org-weit, `llm_usage`- und `knowledge_updates`-Inserts nur via Backend (service_role). PostgREST exponiert `public` (geprüft).

## Als Nächstes (MVP Phase 1)

1. [x] **Auth-Entscheidung (2026-07-03):** enneo nutzt **Microsoft**, nicht Google — und Aleksa ist kein Azure-Admin. Darum MVP mit **Supabase Email+Passwort** (reicht für Demo + Founder-Pitch). **Microsoft-SSO (Azure AD) kommt in Phase 2**, sobald ein Admin den OAuth-Client in Entra ID anlegen kann. Schema ist provider-agnostisch (profiles-Trigger greift bei jedem Auth-Provider).
2. [x] **Node-Backend geschrieben (2026-07-03, Code komplett, noch nicht deployed):** `backend/` — Express + SSE-Streaming, Enni-Tool-Loop mit dem offiziellen `@anthropic-ai/sdk` (bewusste Abweichung vom Agent-SDK: gleiche API, volle Kontrolle über Gedankenkette/Tool-Calls/cost_eur-Persistenz, läuft auf jedem Node-Hosting — Begründung in `backend/README.md`). Tools: `wiki_list_pages`/`wiki_read_page`/`wiki_search` (Supabase) + `gitlab_search_projects`/`_search_code`/`_read_file`/`_list_merge_requests` (read-only REST, ersetzt Attio als ersten Connector). Model `claude-opus-4-8`, Prompt-Caching auf dem System-Prompt, `llm_usage`-Logging mit Euro-Betrag pro Antwort. **Zum Laufen fehlt:** (a) Hosting (Punkt 3), (b) `ANTHROPIC_API_KEY` (Aleksas persönlicher), (c) `GITLAB_TOKEN` (read_api-PAT aus dem enneo-GitLab) + `GITLAB_BASE_URL` — GitLab-Tools antworten bis dahin mit "Connector noch nicht konfiguriert", Wiki-Tools funktionieren sofort. Code ist ungetestet bis zum ersten Deploy (kein lokales Node auf dieser Maschine).
2b. [x] **Wiki-Seeding erledigt (2026-07-03): 303 Seiten live.** Importiert aus claude-team `ai-team/agents/enni-enneo/knowledge/`: `enneo-company`, `enneo-platform-deep-dive`, `product-docs/*` (71), `api-docs/*` (203), `marketing-site/*` (27) — Upsert auf slug via PostgREST, Re-Import jederzeit wiederholbar (Script-Pattern: Batch-POST mit `Prefer: resolution=merge-duplicates`). **Bewusst NICHT importiert:** `aleksa-role.md` (Vertragsdetails), `job-description.md`, `company-os-dust-research.md` (projektintern), `richard-customer-call-learnings.md` + `extraenergie-egfactory-workshop.md` (Aleksas private Call-Notizen — nur nach explizitem OK ins Team-Wiki). **Verifiziert:** Enni beantwortet "Copilot vs. Autopilot" korrekt via wiki_search + wiki_read_page (~0,23 €/Antwort bei großen Seiten — bei Team-Nutzung später Snippets statt Vollseiten erwägen).
3. [x] **Backend LIVE auf Railway (2026-07-03):** `https://enneo-os-backend-production.up.railway.app` — Projekt `enneo-os-backend` (ID `4be1c83c-1c40-4d92-83e3-86f5c2e1afc9`) auf Aleksas Railway-Account (Free Trial, danach 1 $/Monat). Deploy via **Railway CLI** (`railway up --detach --service enneo-os-backend` aus `backend/`), NICHT GitHub-Auto-Deploy — Aleksa ist kein Admin der enneo-AI GitHub-Org, Railway-App kann dort nicht installiert werden. Env-Vars gesetzt: ANTHROPIC_API_KEY (Aleksas, via Dashboard), SUPABASE_URL + SERVICE_ROLE_KEY, ENNI_MODEL, EUR_PER_USD, FRONTEND_ORIGIN. Fixes unterwegs: Node 22 nötig (supabase-js braucht native WebSocket) + Tabellen-GRANTs auf dem Supabase nachgezogen (neue Projekte blocken sogar service_role — bekannte Lesson). **End-to-End-Smoke-Test bestanden:** Enni antwortet gestreamt, ruft wiki_list_pages auf, Conversation/Messages/llm_usage werden persistiert (erste Antwort: 0,019 €). Login-User `aleksa@enneo.ai` angelegt (Email+Passwort).
4. [x] **Frontend gebaut + lokal verifiziert (2026-07-04):** `frontend/` — bewusst **Vanilla HTML+JS ohne Build-Schritt** (kein Node nötig, Netlify-Drop-fähig; supabase-js/marked/DOMPurify via esm.sh). Umgesetzt nach mockup-v5: Login (Email+Passwort), Chat mit SSE-Streaming, Gedankenkette (einklappbar, live), Tool-Rows → Tool-Detail-Panel (echte Inputs/Outputs, nach `done` aus DB hydriert), Kosten pro Antwort + "Heute X €" im Sidebar-Footer, Konversations-Historie (RLS direkt aus Supabase), Wiki-Browser über alle 303 Seiten (Gruppierung + Filter + Markdown-Rendering), Kosten-View (heute/Monat/Anzahl). **E2E im Browser getestet:** Login → Frage → Enni streamt mit 3 Tool-Calls → 0,29 € Meta → Panel zeigt echte Tool-Daten → Wiki rendert. **LIVE auf Netlify: `https://enneo-os.netlify.app`** — seit 2026-07-05 **GitHub-Auto-Deploy** (Aleksa hat das Repo verbunden; jeder Push auf main deployt automatisch). `netlify.toml` im Root setzt `publish = "frontend"` (kein Build-Schritt). Das frühere Drag&Drop ist obsolet. `FRONTEND_ORIGIN` auf Railway enthält die Netlify-URL, CORS verifiziert. Eigene Domain kommt später (Aleksa). **Achtung Backend ≠ Frontend:** Railway deployt weiterhin NUR per CLI (`railway up --detach --service enneo-os-backend` aus `backend/`) — ein git push allein aktualisiert das Backend nicht.
4b. [x] **Spaces nach Dust-Pattern (2026-07-04, echtes DB-Modell):** Migration `0002_spaces.sql` — Tabellen `spaces` (open/restricted), `space_members`, `space_connections`; `wiki_pages.space_id`; Company Data als Seed-Open-Space mit allen 303 Seiten + Connections wiki/gitlab. RLS: Open Spaces sieht jeder, Restricted nur Mitglieder/Ersteller/Admins. Frontend: Sidebar-Baum (Administration → Connections/Tools · Open Spaces aufklappbar → Connected Data/Folders/Websites/Tools · Restricted Spaces), **funktionierendes "+ New"** (Name, Open/Restricted-Toggle, Mitglieder-Checkboxen aus profiles → persistiert), **funktionierender "Add data from connections"-Flow** (Checkbox-Modal → space_connections). E2E getestet inkl. Space-Anlage + Connection-Zuordnung. **⚠️ Noch NICHT:** (a) Enni respektiert Space-Rechte beim Antworten (Backend-Tool-Layer muss wiki/gitlab nach Spaces des fragenden Users filtern — nächster logischer Schritt), (b) Datei-Upload in Folders, (c) OAuth-Connectors (Drive/Attio/Kalender/Mail), (d) Space löschen/umbenennen-UI.
5. [ ] **Wissens-Update-Loop:** Diff-Vorschlag → Freigabe → Wiki-Seite aktualisiert → Audit-Eintrag

2c. [x] **GitLab-Connector LIVE (2026-07-04):** `GITLAB_TOKEN` + `GITLAB_BASE_URL=https://gitlab.com` auf Railway gesetzt. Token-Quelle: Aleksas bestehender PAT **„aleksa-enneo"** aus dem macOS-Schlüsselbund (`security find-internet-password -s gitlab.com -w`), gültig bis **2027-04-30**, aber voller `api`-Scope — **TODO irgendwann:** dedizierten read-only-Token (`read_api`) erstellen und tauschen. enneo-GitLab ist gitlab.com, Namespace `enneo` (Monorepo `enneo/enneo`, dazu ops-fe, cortex, mind, io-proxy, deployment, analytics …). **E2E verifiziert:** Enni listet die enneo-Projekte via `gitlab_search_projects` korrekt auf.

6. [x] **Kosten-Optimierung (2026-07-04): RAG + Caching + Modell-Wahl.** (a) **RAG:** Migration `0003_rag.sql` (pgvector, `wiki_chunks` 1.303 Stück à ~2.500 Zeichen, hnsw-Index, RPC `match_wiki_chunks`), Edge Function `embed` (gte-small 384 Dim., eingebaut/kostenlos — ⚠️ CPU-Limit ~2s: max. 2 lange Texte pro Call, Indexer-Script mit B=2). Neues Enni-Tool `wiki_semantic_search` (Top-8-Chunks statt ganzer Seiten), System-Prompt: semantisch zuerst, read_page nur wenn nötig. (b) **Prompt-Caching:** ein wandernder cache_control-Breakpoint auf der letzten Message pro Iteration (alte Marker werden entfernt — max. 4 erlaubt!). (c) **Modell-Dropdown im Chat** (Opus 4.8 Default / Sonnet 5 / Haiku 4.5), localStorage-persistiert, Backend-Whitelist. **Gemessen (gleiche Frage "Copilot vs. Autopilot"):** vorher 0,23 € → Opus+RAG **0,085 €** → Sonnet+RAG **0,041 €**. Folge-Nachrichten in derselben Konversation werden durch Cache-Reads nochmal deutlich billiger. Re-Index nach Wiki-Änderungen: `scratchpad/index_rag.py`-Pattern (Chunks löschen + neu embedden) — TODO: als Cron/Trigger automatisieren.

7. [x] **Context Compaction nach Dust-Muster (2026-07-05):** Migration `0004` (message_role + 'compaction'). Backend: `buildHistory()` schneidet am letzten Compaction-Anker (Zusammenfassung + neuere Messages gehen ans Modell, Originale bleiben gespeichert/sichtbar), `POST /api/compact` (Haiku fasst zusammen, ~0,004 €, llm_usage-Eintrag). Frontend: Kontext-Ring im Composer (Budget 60k Tokens, ~Zeichen/4), Schwellen wie Dust: ab 33 % Klick-zum-Komprimieren, ab 70 % Warnung, ab 80 % Senden blockiert; "✓ Kontext komprimiert"-Marker mit aufklappbarer Zusammenfassung. **E2E verifiziert** inkl. Folge-Frage aus der Zusammenfassung. **Bugfix nebenbei:** Haiku kann kein adaptive thinking — Parameter wird jetzt modellabhängig gesetzt.

8. [x] **Composer-Redesign + Anhänge + Diktat (2026-07-05):** Composer als große Glass-Karte im Navigations-Stil (Chips-Zeile, Autosize-Textarea bis 180px, Tasten-Leiste: 📎 Anhang / 🎙 Diktat / Kontext-Ring [immer sichtbar] / Modell-Taste / erhabener Senden-Button). **Anhänge:** Excel, CSV, JPEG, PNG, PDF — max. 4 Dateien à 10 MB; Bilder als image-Block, PDF als document-Block, CSV als Text, Excel wird im Backend via `xlsx` zu CSV konvertiert (`backend/src/attachments.js`); Datei-Inhalt geht nur im Upload-Turn ans Modell, im Verlauf bleibt Marker + `messages.attachments` (Metadaten, Migration 0005) — bewusst gegen Kosten-Explosion. **Diktat:** Web Speech API (Chrome/Edge/Safari; Firefox nicht), Default Deutsch, Sprachwechsel DE/EN über Link im Hinweis unterm Composer, rote Puls-Animation bei Aufnahme. **E2E getestet:** CSV-Upload über die UI (Enni beantwortet Frage zur Datei korrekt), Bild-Pfad via API (Farb-Erkennung). Diktat nur Code-Review (Mikro in Headless nicht testbar — Aleksa bitte einmal real klicken).

9. [x] **Pods — Projekt-Räume nach Dust-Muster (2026-07-05):** Migration `0006` (pods, pod_members, pod_tasks, pod_files, conversations.pod_id, messages.author_id, `pod-files` Storage-Bucket, `is_pod_visible()` SECURITY DEFINER). **Pod-Seite mit 4 Tabs:** Konversationen (geteilt — jeder im Pod liest/schreibt mit, Peer-Messages zeigen Absender-Namen), Aufgaben (anlegen, abhaken, **▶ "Enni arbeiten lassen"** öffnet Modal mit optionaler Nachricht → startet Pod-Konversation und verlinkt sie mit dem Task = Audit-Trail), Dateien (Upload in `pod-files`, Download via Signed-URL), Einstellungen (Name/Beschreibung/**Instructions for Agents**/Open-Toggle). Sidebar-Pods-Liste + "＋"-Modal (Open/Restricted + Mitglieder). **Backend:** Pod-Konversationen laufen mit Absender-Attribution im Verlauf + Pod-Instructions als zweiter System-Block. **E2E verifiziert:** Pod anlegen → Instructions speichern → Aufgabe anlegen → ▶ → Enni antwortet im Pod-Kontext, Task auf in_progress + Konv-Link in DB bestätigt. **RLS-Bug gefixt:** pods-SELECT-Policy inline statt `is_pod_visible(id)` (Selbst-Requery sieht INSERT..RETURNING-Zeile nicht). Test-Pod "STAWAG Projekt" bleibt für die Demo stehen. **Noch offen:** Enni greift in Pod-Konversationen noch NICHT automatisch auf Pod-Dateien zu (Datei-Bibliothek ist bisher nur Ablage/Download; Anbindung an den Tool-Layer = nächster Schritt); Realtime-Updates (andere sehen neue Messages erst nach Reload).

10m. [x] **Pods als Team-Chat nach Dust-Muster (2026-07-08):** (a) **@enni-Semantik**: In Pod-Konversationen antwortet Enni NUR bei @enni-Erwähnung (Regex-Gate im Backend `/api/chat`); ohne Tag wird die Nachricht nur persistiert (Team-Chat, kein LLM-Call, 0 €) — Auto-Titel läuft trotzdem. Frontend rendert dann keinen Enni-Container; Composer-Placeholder wechselt in Pods auf "Nachricht ans Team — @enni ruft Enni …". @Erwähnungen werden farblich hervorgehoben (`mentionize()`). (b) **Voller Pod-Kontext bei @enni**: neue `pod_`-Tools in `src/tools/pod.js` — pod_list_tasks, pod_list_files, pod_read_file (CSV/Text direkt, Excel→CSV; PDF/Bilder mit Hinweis), pod_list_conversations, pod_read_conversation. Damit ist das offene Item "Enni greift auf Pod-Dateien zu" erledigt. System-Prompt-Block weist Enni auf die Tools hin. (c) **Pod-Startseite**: Direkt-Input ("Schreib dem Team … — @enni ruft Enni dazu") ersetzt den "+ Neue Pod-Konversation"-Button (Enter/Start → startet Konversation und sendet), plus Suchfeld über die Pod-Konversationen (Client-Filter). **E2E:** Team-Nachricht ohne Tag → kein Enni; "@enni was steht auf der Aufgabenliste…" → pod_list_tasks + pod_list_files mit echten Daten. Gotcha beim Testen: direkt nach `railway up` kann der ALTE Container noch Requests bedienen (Deploy-Race) — vor E2E kurz warten. Test-Konversationen danach aus dem STAWAG-Pod gelöscht. **Noch offen:** Mention-Autocomplete (@-Dropdown mit Pod-Mitgliedern), Realtime für Team-Chat (andere sehen Nachrichten erst nach Reload) — jetzt wichtiger, weil Pods echte Team-Chats sind.

10l. [x] **Echte Tool-/Connection-Verknüpfung via MCP + enneo-Brand-Icon (2026-07-08):** (a) **Generischer MCP-Connector-Layer**: Migration `0010_connectors` (Tabelle `connectors`, Token write-only via Spalten-Grant — authenticated darf die token-Spalte nicht selecten), Backend `src/tools/mcp.js` mit offiziellem `@modelcontextprotocol/sdk` (Streamable-HTTP-Transport, optionaler Bearer-Token). Tools werden live geladen (60s-Cache, Fehler pro Server nicht-fatal) und Enni pro Turn unter `mcp__{slug}__{tool}` bereitgestellt; Ausführung via `tools/call`. Endpoints `POST/DELETE /api/connectors` (nur Admins, Verbindungstest beim Anlegen). Frontend: "+ Eigenes Tool verknüpfen" (Tools-Seite) und "+ Connection verknüpfen" (Connections-Seite) → Modal (Name, URL, Token) → Reihe mit Host + Tool-Count + Trennen-✕ (Hover, Admin). **E2E:** DeepWiki-MCP (`https://mcp.deepwiki.com/mcp`, 3 Tools) über die UI verknüpft → Enni ruft `mcp__deepwiki__read_wiki_structure` auf und liefert echte Repo-Doku (Achtung: DeepWiki-Calls können ~2 Min dauern). DeepWiki bleibt als Demo verbunden. **Grenzen:** nur Streamable-HTTP-MCPs mit offenem Zugang oder Bearer-Token — OAuth-MCPs (Notion, Linear …) brauchen einen späteren OAuth-Flow; MCP-Tools laufen ohne Freigabe-Karte (bewusst verknüpfte Server), Delete-Endpoint implementiert aber nicht E2E-getestet. (b) **enneo-Brand-Icon**: offizielles Marken-SVG von enneo.ai geladen und auf Brand-Lila `#7B5AE2` (= unser `--lila`!) umgefärbt → `icons/enneo-icon.svg`, gesetzt als Favicon + Logo der Enneo-Plattform-Zeilen (ersetzt das DuckDuckGo-Favicon-PNG).

10k. [x] **Modell-Auswahl + Auto-Titel v2 + Logout-Icon (2026-07-08):** (a) **Fable 5** (`claude-fable-5`, $10/$50 pro MTok) in Modell-Whitelist, Pricing und Dropdown — unser adaptive-Thinking-Setup ist Fable-kompatibel (thinking dort immer an, `{type:adaptive, display:summarized}` wird akzeptiert; `refusal`-stop_reason wird vom bestehenden Error-Pfad abgefangen). (b) **Opus 4.8 ist Default bei jedem Chat-Start** — `newConversation()` resettet das Dropdown, localStorage-Persistenz entfernt; innerhalb einer Konversation bleibt die Wahl. (c) **Auto-Titel v2:** Haiku (günstigstes Modell) analysiert NUR die erste Nachricht und formt einen 1-5-Wort-THEMA-Titel (nicht die Frage wiederholen); läuft PARALLEL zum Enni-Turn (Promise startet vor runEnniTurn, Emit nach done) statt sequenziell danach. E2E: "Wie heißen die beiden Gründer…" → Titel "Enneo Gründer". (d) Abmelden-Button ist jetzt ein Logout-Icon (Stroke-SVG) statt Text.

10j. [x] **Connections/Tools im Codex-Look (2026-07-08):** Beide Admin-Seiten als flache Logo-Reihen (`.crow`): 36px-Logo-Tile (weiß, auch im Dark Mode — Brand-Logos brauchen hellen Grund), Name + EIN Satz, Status rechts (grüner Punkt "Verbunden/Aktiv" bzw. muted "Phase 2"). Echte Brand-SVGs von svgl.app in `frontend/icons/` (gitlab, notion, slack, google-drive, outlook, google-calendar) + Favicon-PNGs für Attio/enneo (via icons.duckduckgo.com — Google-Favicon-Service war aus der Sandbox nicht erreichbar). Tools konsolidiert: Enneo-Plattform (Aktiv — propose_write), Outlook (Mail+Kalender in EINEM Eintrag, enneo ist Microsoft-Shop), Attio, "+ Eigenes Tool (MCP)". Intro-Texte beider Seiten auf einen Satz gekürzt. Connected-Data-Liste + Add-Modal zeigen dieselben Logos. Grid-Glass-Karten + Stroke-Icons ersetzt.

10i. [x] **Antwort-Copy (2026-07-07):** Subtiler Copy-Button in der Meta-Zeile jeder Enni-Antwort (neben den Kosten, sichtbar bei Message-Hover) — kopiert NUR den Antwort-Text (Markdown-Source), nicht die Gedanken. Gemeinsame `copyText()`-Helper-Funktion für Message- und Code-Block-Copy.

10h. [x] **Code-Copy + Profil-Bearbeitung + E-Mail-Domain-Zwang (2026-07-07):** (a) **Copy-Button auf Code-Blöcken** — `enhanceCode()` hängt an jedes `pre` (Enni-Antworten + Wiki-Seiten) einen Hover-Copy-Button mit ✓-Feedback; Clipboard-API mit execCommand-Fallback. (b) **Profil-Modal** (Klick auf Avatar/Name im Sidebar-Footer): Anzeigename, Profilbild (PNG/JPEG/WebP ≤3 MB → public Bucket `avatars`, alte eigene Dateien werden ersetzt), Passwort ändern (≥8 Zeichen, Wiederholung), E-Mail read-only. Migration `0009_profile_editing`: `profiles.avatar_url`, Bucket + Storage-Policies (Upload nur eigener `{uid}-`-Pfad), **`handle_new_user`-Trigger erzwingt @enneo.ai** — Signup mit fremder Domain schlägt DB-seitig fehl (verifiziert: gmail-Adresse → Exception). Avatar-Upload E2E getestet (Testbild danach wieder entfernt). (c) Spaces-UI: neue Folder-Gruppe "Enneo-API-Rezepte" — die `enneo-api/`-Seiten waren vorher im Browser unsichtbar. Offen: Copy-Klick einmal real im Browser gegentesten (headless kein Clipboard-Fokus).

10g. [x] **Pod-Lifecycle + De-Box-Pass (2026-07-06):** (a) **Pod verlassen** — Migration `0008_pod_leave` erweitert `pm_delete`-Policy um Selbst-Löschung (`user_id = auth.uid()`); Link in Pod-Einstellungen, sichtbar nur für Nicht-Ersteller-Mitglieder. (b) **Pod löschen** — nur Ersteller/Admin (Policy existierte), Link in Einstellungen, löscht vorher die Storage-Objekte in `pod-files` (DB-Rows cascaden, Bucket-Files nicht). E2E: test pod gelöscht. Private Chats löschen gab es schon (Sidebar-Hover ✕). (c) **De-Box:** funktionslose Badges raus ("nur du" im Chat-Header, "Open/Restricted" im Pod-Header — Restricted-Info trägt das Lock in der Sidebar), alle `.glass list-panel`-Karten zu flachen Listen mit Hairlines, Empty-States als schlichter Text statt Box (`.empty-plain`). Aleksas Design-Regel dazu: **keine funktionslosen Tags/Badges, keine Boxen um Listen/Empty-States — Boxen = AI-Slop-Look + Platzverschwendung.** Bei allen künftigen UI-Builds beachten.

10f. [x] **Darstellungs-Paket (2026-07-06):** (a) **Streaming-Absätze** — Text aus neuen Tool-Loop-Iterationen bekommt `\n\n`-Separator (backend agent.js), Zwischensätze kleben nicht mehr aneinander. (b) **Auto-Titel** — Haiku benennt jede neue Konversation nach dem ersten Austausch (~0,001 €, geloggt in llm_usage), SSE-Event `title` aktualisiert Header + Sidebar live. (c) **Sidebar-Zeitgruppierung** (Heute / Diese Woche / Älter) + **Umbenennen/Löschen** per Hover-Icons (inline-Rename, Delete mit confirm; messages CASCADE, llm_usage SET NULL — geprüft). (d) **Dark Mode** — komplette CSS-Umstellung auf Semantik-Variablen (--bg/--surface/--code-bg/--hover/--press/--key-hi/lo/--cta-*), `:root[data-theme=dark]`-Overrides, Toggle im Sidebar-Footer, Anti-Flash-Init inline im head, localStorage `enni-theme`. (e) **Loading-Skeletons** beim Konversations-Öffnen. (f) Tool-Output-Clip 2.000 → 20.000 Zeichen (Tool-Panel zeigt jetzt volle Outputs). Alles E2E im Preview verifiziert (Light + Dark Screenshots, Auto-Titel live gesehen). **Mobile bewusst NICHT angefasst** — kommt laut Aleksa erst wenn Desktop fertig ist.

10e. [x] **Enni-IQ-Upgrade: Plugin-Skills als API-Rezepte im Wiki (2026-07-06):** Root Cause für schwache Instanz-Antworten (Beispiel: Telefonnummern-Chat — Enni riet Endpoints, kassierte 405, gab auf): Das offizielle Claude-Code-Plugin trägt sein API-Wissen in 16 Skill-Markdowns (~2.750 Zeilen), Enni hatte nur einen Mini-Cheatsheet. Fix: 15 Skills (alle außer browser-jwt) als Wiki-Seiten unter Slug `enneo-api/{thema}` importiert (Quelle: `~/.claude/plugins/marketplaces/claude-plugin/skills/`, Boilerplate durch enneo-OS-Tool-Mapping ersetzt), 43 RAG-Chunks embedded, System-Prompt-Regel: "BEVOR du enneo_api_get gegen unbekannte Endpoints aufrufst, lies das Rezept (wiki_read_page enneo-api/{thema}); 405/404 heißt Rezept nachschlagen, nie mehrfach blind raten." **Retest der zuvor gescheiterten Frage bestanden:** Telefonnummern + Inbound/Outbound-Richtung pro Leitung korrekt via /report/telephonyLines + /telephony/getRouting (Leitung 11 „Voicebot Emma + ACD" in+out, Leitung 12 nur Outbound). **Wartung:** Wenn enneo das Plugin updated → Skills neu importieren + Chunks re-embedden (Import-Script-Pattern in diesem Eintrag, Chunks löschen via `slug=like.enneo-api/*`).

10d. [x] **Enneo-Writes mit Freigabe-Gate (2026-07-06):** Enni kann Instanzen jetzt ÄNDERN — aber nur über den Vorschlag-Mechanismus. Migration `0007_enneo_write_proposals` (Tabelle mit RLS: select für authenticated, Schreiben nur service_role). Neues Tool `enneo_propose_write` (POST/PUT/PATCH, DELETE grundsätzlich gesperrt) speichert einen Vorschlag statt auszuführen; Frontend rendert eine **Freigabe-Karte** unter der Enni-Antwort (Summary, Method+Path+Instanz, Body-Preview, Ausführen/Ablehnen). Erst `POST /api/enneo-write/:id/approve` (User-JWT) führt den echten Mind-API-Call aus — Audit-Trail: proposed_by, approved_by, executed_at, result. **Instanz-Auflösung jetzt universell:** jeder Kurzname wird zu `{name}.enneo.ai` (Regex-validiert), keine Allowlist mehr — Default weiter aleksa-dev via `ENNEO_INSTANCES`. **E2E verifiziert:** "Leg auf aleksa-dev einen Tag 'Enni Test' an" → Vorschlag-Karte "Wartet auf Freigabe" → Klick Ausführen → `{"success":true,"id":70}` → Karte "✓ Ausgeführt", Audit-Row komplett. Headless-Login-Trick für Tests: Admin `generate_link` (service_role) → `token_hash` → `/auth/v1/verify` → Session in localStorage.

10b. [x] **Enneo-Plattform-Connector (2026-07-06):** `backend/src/tools/enneo.js` — 4 read-only Tools gegen die Enneo Mind-API (`https://{instanz}/api/mind`, dieselbe API wie das offizielle Claude Code Plugin): `enneo_ticket_search` (POST /ticket/search mit Filtern), `enneo_ticket_get`, `enneo_settings_search` (Zugangsdaten werden per Regex geschwärzt), `enneo_api_get` (generischer GET-Pass-through für /customer, /aiAgent, /intent, /activity …). Auth: universeller Production-Token (`ENNEO_TOKEN` auf Railway, Quelle: claude-team `ai-team/agents/enni-enneo/.env`). Instanz-Allowlist via `ENNEO_INSTANCES` (aktuell nur `aleksa-dev.enneo.ai` — weitere Instanzen = Env-Var erweitern + redeploy). System-Prompt um Enneo-Sektion + Endpoint-Cheatsheet + „Zugangsdaten nie ausgeben"-Guardrail erweitert. **E2E verifiziert:** „3 neueste Tickets auf aleksa-dev" → enneo_ticket_search → echte Tickets (2226-2228), 0,04 €. Hinweis: Das Plugin hat nur 4 echte MCP-Tools — die 13 „Kategorien" sind Skills (Markdown-Anleitungen für die REST-API); unser `enneo_api_get` deckt denselben Long-Tail ab.

10c. [x] **Design-Declutter (2026-07-06):** Emoji-Icons komplett raus (Sidebar-Chats, Pods, Spaces-Tree, Badges, Modals, Tool-Rows, Connections). Text trägt die Hierarchie; einzige Ausnahme: dezentes Lock-SVG für Restricted-Pods/-Spaces. Tools-Cards mit Stroke-SVGs statt Emoji. `.claude/launch.json` für lokalen Static-Preview (python http.server) hinzugefügt.

10. [x] **SPA-Routing (2026-07-05):** echte URLs `/chat`, `/chat/:id`, `/spaces`, `/spaces/tools`, `/spaces/connections`, `/admin`, `/pod/:id` — History-API-Router in app.js (`syncUrl`/`route`), Netlify-Redirects in `netlify.toml` + `frontend/_redirects`. Zurück-Button + Deep-Links E2E verifiziert, alle Routen live 200.

## Als Nächstes (Priorität vor der Founder-Demo Mi 08.07.)

1. [ ] **Wissens-Update-Loop (Punkt 5 unten — der Kern-Differenzierer!):** Enni schlägt nach Konversationen Wiki-Diffs vor (`knowledge_updates`-Tabelle existiert seit 0001), Mensch reviewt/übernimmt, Audit-Eintrag. UI: Learn-Card im Chat (siehe mockup-v5 „WISSENS-UPDATE") + Review-Liste.
2. [ ] **Demo-Vorbereitung:** Demo-Drehbuch (Kosten-Story: Euro pro Antwort → Modellwahl → RAG → Compaction; Pods mit Task-▶; Spaces), Passwort-Änderungs-UI oder neues Passwort für Aleksa.
3. [ ] Enni-Tool-Layer: Space-Rechte durchsetzen + Pod-Dateien lesbar machen.
4. [ ] Realtime (Supabase Realtime auf messages für Pod-Konversationen).

## Offene Entscheidungen

- [x] **Anthropic-API-Account (entschieden 2026-07-03):** MVP läuft mit Aleksas persönlichem API-Key — nur Test + Team-Demo, keine Produktion, Kosten minimal und via `llm_usage` transparent. enneo hat bereits einen Unternehmens-API-Account, aber Aleksa hat (noch) keinen Konsolen-Zugriff. Sobald Team-Testing startet → Firmen-Key von enneo. Key kommt als Env-Var ins Backend-Hosting, nie ins Repo.
- [ ] Founder-Pitch-Termin: MVP-Demo als Kernstück der Team-Präsentation
- [ ] Block-Editor-Library für Wiki-Phase-2 (BlockNote vs. Tiptap — nicht selbst bauen)

## Bewusst NICHT im MVP

Pods, Block-Editor, Connectors-UI, Admin-Dashboards, Routinen/Scheduler, Slack-Anbindung.
